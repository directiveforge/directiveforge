# Feedback — Lifecycle UX Overhaul deferrals (2026-07-12, branch ux-lifecycle)

**Context**: deferral ledger for the lifecycle UX overhaul (`prompts/dispatch/UX_LIFECYCLE_ARCHITECT_PROMPT.md`,
executed 2026-07-12 on branch `ux-lifecycle`). Not a project field report — a thematic feedback
file in the 2026-07-10-pre-hn-hardening precedent. Each row is owed a disposition at the next
release close (zero-silently-dropped gate). Same-day appends only after that, per house rule.

## Suggested Changes (deferral rows)

| # | Deferred item | Why deferred | Evidence | Home / when |
|---|---|---|---|---|
| UXD-1 | Scope-split cost disclosure in DF-RUN-CONTRACT ("Claude-Code-only: ~X; both: ~Y" — architect prompt W1.5.4) | 2 of the 3 recorded runs were already Claude-only and the sole "both"-scope run (greenfield-next, 273k) was the CHEAPEST — a scope split from n=3 is a dishonest cross-fixture comparison, blocked by hard rule 3 (measured claims only) | `harness/results/2026-07-03-baseline/*/run-metadata.json` + per-fixture ide_scope in the generated manifests | Collect ≥2 single-scope vs both-scope runs on the SAME fixture, then add the split line to the contract block (all three surfaces at once) |

*(Rows below appended same-day at wrap, from the W5/W7 runs — sources:
`harness/results/2026-07-12-upgrade-survival/RESULT.md` + `harness/results/2026-07-12-ux-verify/RESULT.md`.)*

| # | Deferred item | Why deferred | Evidence | Home / when |
|---|---|---|---|---|
| UXD-2 | Script the survival runner (`upgrade-survival.workflow.js`, parameterized per l2-delta: kit/scratch/outBase/date args, `*baseline*` refusal, scripted adjudication answers as args) | Mid-pass human gate did not fit the batch pattern before the protocol was proven; its precondition — "protocol survives two runs" — is NOW MET (W5 + W7 re-run) | RUNBOOK §3bis; the two runs | v0.20.1+ / next harness round |
| UXD-3 | Mid-phase-kill robustness (half-written file at kill time) | Boundary-stop was chosen to preserve the billing report LC-2 needs; mid-phase kill untested by construction | UX-REHEARSAL-PROTOCOL §2 pre-registered limitation | Next rehearsal round |
| UXD-4 | LC-2 resume carve-out (mirror LC-1's "initial segment only" for resumed runs) | Amending a metric after grading a run against it = post-hoc tuning (HARNESS-SPEC §2.3); ships only as a NEW dated spec version, no re-grade of the 2026-07-12 verdicts | ux-verify RESULT F1 | Next SPEC version (v1.3) at next release close |
| UXD-5 | One clean single-continuous Starter run on the orchestration-clock basis to validate the ~18–21 min / ~270–330k figures | The rehearsal was an instrumented resume test; seg-1 alone (phases 0–3) hit 24.5 min / 327k on that basis — basis mismatch vs the interactive baseline is unresolved | ux-verify RESULT F2 | Before the figures carry further public weight |
| UXD-6 | Fix kit decision-skill TEMPLATES: S21 phantom-route "at this tier" markers (14 routes, 5 skills) + S22 description 4-gram collision — **the live instance of residual N5** (`DISPOSITIONS-v0.20.0.md`): `templates/skills/decision/confidence-calibration/SKILL.md` and `templates/skills/decision/reversibility-check/SKILL.md` share the 4-gram "the agent is about" in their frontmatter `description` fields. Confirmed live 2026-07-12 when the W7 rehearsal's Phase 5 hit §22 on a fresh install and had to reword the *installed* copy of `reversibility-check`'s description (and drop the phantom council-3-voice route for sub-Advanced tiers) — every fresh install inherits the collision until the source templates are fixed. | Template fixes belong to the v0.20.1 release train, not the lifecycle branch (launch freeze; templates ship publicly). N5 is routing-irrelevant today (affected skills' L1.2 = 1.0), but the fix REWORDS `description` — a trigger-behavior change — so it is NOT a free doc edit. | ux-verify RESULT F3; rehearsal Phase 5 fixed both in-run on the install copies; `DISPOSITIONS-v0.20.0.md` N5; README limitations row | **v0.20.1, WITH an L1.2/L1.3 delta re-measure of the affected skills** (`l1-delta` runner, per HARNESS-SPEC pre-registration): reword the 2 templates at source so `generator/VALIDATION_CHECKLIST.md` §22 passes on a fresh install, re-run §22 over all `templates/skills/**/SKILL.md` pairs to catch other collisions, then re-measure L1.2/L1.3 on `confidence-calibration` + `reversibility-check` to prove the reword did not regress the current 1.0 activation before publishing the fix. Record in `feedback/` per `feedback/README.md`; commit `fix:`. |
| UXD-7 | LC-1 ≤180 s presentation band vs the contract's qualitative promise (394 s measured, promise met) | Band is presentation-only; re-banding post-run = tuning; footnote/re-band rides the next spec version | ux-verify RESULT F6 | Next SPEC version |
| UXD-8 | Phase-4 banner guidance: a phase that writes the empty `PRE-EXISTING-MODIFIED.txt` sentinel should not read "skipped" | Cosmetic self-contradiction; uninstall classifies the sentinel correctly regardless | ux-verify RESULT F7 | v0.20.1 doc pass |
| UXD-9 | UPGRADE_MODE doctrine additions from the model-split observations: (a) protected-twin coupling — default for the unprotected mirror of a §3a-protected file (Opus HELD, Sonnet UPGRADED); (b) UPGRADE application when the recorded `placeholders` map lacks the newer template's analysis placeholders (fold-in vs raw substitution — both runs converged on fold-in) | Real doctrine gaps surfaced by run divergence, not defects; deserve a deliberate doc decision, not a branch-side patch | ux-verify RESULT non-blocking notes; both apply agents' returns | v0.20.1 |
| UXD-10 | WORKFLOW_HEALTH_AUDIT manifest lens: one-line clarification that generation-time hashes + `owner_customized: false` on an owner-edited file is the by-design state the upgrade 2×2 CONSUMES (not "stale in the way that defeats its purpose") | The W5 audit misread a by-design property as its top 🚨 finding; harmless this run (dry-run reclassified correctly) but noise-generating | upgrade-survival RESULT non-blocking notes | v0.20.1 |
| UXD-11 | UPGRADE_MODE §5 wording: state explicitly that keep-mine rows get `template_sha256` refreshed to the current kit template (so the next pass sees differ/match → KEEP+flag, stable) | Both apply agents did this correctly but §5's text names it only via "new template_sha256 where the template moved" — implicit for keep-mine rows | upgrade-survival RESULT non-blocking notes | v0.20.1 |

*(UXD-5 disposition below, appended 2026-07-13 — one day after this file's creation date, out of the
normal release-close cadence. Deliberate: UXD-5's own "Home / when" pre-registered it as
time-sensitive ("Before the figures carry further public weight"), and
`launch/OPUS-SUCCESSION-PROMPT.md` gates Wednesday's ux-lifecycle→main merge messaging on this
verdict. Source: one clean, single-continuous Starter-tier run, `general-node` preset (Express
fixture, previously untimed), orchestration-clock basis — `date -u` brackets around the generator
subagent's dispatch (T0) and return (T1), the same instrument `ux-verify RESULT F2` used. Runner:
Sonnet-class (this session), per explicit non-invasive-Monday-spot-check instruction. NOTE —
model-class caveat: `harness/HARNESS-SPEC.md` §3 pins the formal generator-runner model to Opus 4.8
("the model class real users run"); both the 2026-07-03 baseline and the F2 rehearsal are Opus.
This run is a fourth, differently-modeled data point — informative, not a HARNESS-SPEC-compliant
re-measurement of the baseline.)*

| Row | Measured (orchestration-clock, single-run, Sonnet-class) | Evidence | Verdict |
|---|---|---|---|
| UXD-5 | **27 min 28 s / 32 files / ~367k tokens** — full run, Phase 0 → Phase 9, 0 operator questions | dispatch T0 `2026-07-13T14:17:33Z` → return T1 `2026-07-13T14:45:01Z`; internal phase-banner span (first banner → Phase 9 banner) `14:24:34Z`–`14:43:58Z` = 19m24s, meaning **~7m01s of the total sat before the first phase banner even printed** (KB reads + dispatch overhead the harness's internal per-run timing never captured) plus ~1m03s of post-Phase-9 report composition; `.df-setup-state.json` absent, `.ai-kit-manifest.json` present/valid (31 files, 3/3 hash spot-checks matched), `docs/AI-WORKFLOW-BRIEF.md` present (73 lines, early mtime), `PRE-EXISTING-MODIFIED.txt` present (empty), `ide_scope: ["claude-code"]`, zero `.cursor/**` artifacts — all 6 completion sanity checks passed | **MATERIALLY OVER — 27m28s clears the 21-min ceiling by ~6m28s (~31% over); 367k tokens also clears the 330k ceiling. Flag for Wednesday: the public "~18–21 min / ~270–330k" claim (`README.md:52`, `QUICK_START.md:5`, both `launch/stage/directiveforge-public/` mirrors) needs updating or re-scoping before it carries further public weight, not a clean validation.** Caveat: the subagent confirmed the checkpoint file was correctly *absent* at completion but did not literally append the per-phase JSONL checkpoint lines the Run Protocol describes mid-run (single unbroken session, no resume exercised) — end-state contract met, but this is a minor fidelity gap in how faithfully the Run Protocol's own mechanic was exercised, not a defect in the timing measurement. |
