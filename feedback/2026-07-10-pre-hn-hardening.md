# Feedback — pre-HN hardening findings (2026-07-10)

**Source:** internal pre-launch review (fresh-context architect pass) + a live field upgrade of a
kit-generated project (content/editorial repo, kit v0.16.2 → v0.20.0, reconstructed-manifest path —
everything adjudicate-only). Project identified by bucket only, per the sanitization rules.

| # | Finding | Disposition |
|---|---|---|
| PH-1 | Kit-voice unmeasured multipliers survived the v0.20.0 README sweep in five files (KB-01 §11.7, KB-02 §6.6, WORKFLOW-CLAUDE-CODE §9/§11/§15, VALIDATION_CHECKLIST §11, PROJECT_SETUP_PROMPT cost tips): "2-3x quality", "40-60%", "~50%", "25x cheaper" — contradicting the published "measured, not claimed" rule. | FIXED this commit — numbers deleted or made price-derived/attributed. research/ keeps third-party figures as attributed source material; frozen harness artifacts untouched (historical records). |
| PH-2 | Model-state stale in the kit's own doctrine: Fable 5 still written as SUSPENDED (restored 2026-07-01; usage-credits-only since 2026-07-07); Sonnet 5 GA (2026-07-01) absent from routing docs. A freshness-first kit was stale about its own strongest model. | FIXED (minimal state refresh: CLAUDE-SURFACE-ROUTING §1a/§1b, KB-02 §4.8, KB-04 §4.1, KB-06). Full kit-wide default-model routing rewrite stays queued as 2026-08-01 monthly integration priority 1. |
| PH-3 | UPGRADE_MODE still emitted legacy `AI-KIT-UPGRADE-PLAN-*.md` output filenames post-rename (found by the live field upgrade). | FIXED — output renamed `UPGRADE-PLAN-*.md`; `.ai-kit-manifest.json` deliberately KEPT (load-bearing: upgrade mode matches existing installs by that filename). |
| PH-4 | Cross-project lock-bleed hazard: a hand-drafted upgrade prompt imported compliance locks belonging to a *different* project of the same operator; caught in review before execution. Root cause: locks written from operator memory instead of discovered from the target repo. | DEFERRED (post-launch, named home): add a "locks are DISCOVERED from the target repo's DECISIONS.md/rules — never imported from operator memory or another project" rule to UPGRADE_MODE and PROJECT_SETUP_PROMPT Phase 0/1. |
| PH-5 | Dirty-tree hazard: launch-critical prompts sat untracked in the working tree for days (one `git clean` from loss), violating the W0-commits-at-W0 lock. | FIXED — all committed this session; lesson re-affirmed. |
| PH-6 | Provenance freeze-row lag: the RELEASES.md row recording the *actually pushed* public tree (`8206c4c`, built at `d1efd8a`) was uncommitted; HANDOFF still pointed at the prior freeze. The W5-verified tree differs from the pushed tree by the same-day provenance rebuild. | FIXED — row committed, HANDOFF pointers refreshed; future freezes commit the row in the same action as the build. |
| PH-7 | Flagship command documented in its non-working bare form: the README quickstart + QUICK_START + KIT-INSTALL-PIPELINE advertised `/report-friction`, but under a plugin install the working invocation is `/directiveforge:report-friction` — bare `/report-friction` returns "Unknown command". QUICK_START further claimed namespacing applies to skills but not commands, which is empirically false. Found by the clean-machine smoke on a current CLI (2.1.206), not caught by `plugin validate` (which checks the manifest, not doc invocation strings). | FIXED — invocations namespaced, first mention in each doc shows the real form once, the false skills-only-namespacing sentence corrected. Generated project-level command (installed bare by the generator) left as-is; that install path invokes it bare correctly. |
| PH-8 | Discoverability gap (upstream Claude Code, not the kit): `claude plugin list` (plain and `--json`) shows only plugin-level metadata (id/version/scope/enabled), never the bundled skills/commands — a first-time user can't see what surface the plugin added without inspecting its cache dir. The skills/commands ARE loaded and functional (verified via `slash_commands` in a `-p --output-format json` session). | DEFERRED (upstream) — not a DirectiveForge defect; candidate to raise against the Claude Code CLI. Noted so a user reading `plugin list` output isn't misled into thinking nothing installed. |
| PH-9 | First-run friction (upstream Claude Code, not the kit): the first interactive `claude` launch runs onboarding (theme picker → login-method picker) before accepting any message, even when `CLAUDE_CODE_OAUTH_TOKEN` is already set. Surfaced while scripting the clean-machine trigger tests. | DEFERRED (upstream) — environment/onboarding behavior of the CLI, not the plugin; recorded for completeness of the clean-machine field evidence. |
| PH-10 | A dispatch instruction for the generator-cost-disclosure edit (public commit #4) specified unmeasured figures — "15–45 minutes" and "at most one operator question" — while citing `harness/results/2026-07-03-baseline/` as the source. The harness data actually shows a tight 17.9–21.3 min cluster across all three fixtures and zero live operator questions (pre-scripted profiles). Caught during plan-mode verification before either claim shipped. | FIXED — corrected to the measured range (~18–21 min, ~270–330k tokens, zero questions in the recorded runs / roughly one if ambiguous) before the QUICK_START.md/README.md edits landed. |
| PH-11 | QUICK_START.md's pre-existing "Time: 15-45 minutes... Advanced ≈ 40-45 min" line was itself an unmeasured claim, shipped since v0.20.0, and contradicted by the same harness data (the Advanced-tier fixture ran 21.3 min, not 40-45). The PH-1 truth-debt sweep missed it because it grepped multipliers/percentages ("2-3x", "40-60%"), not duration ranges. | FIXED — replaced with the measured range in the same edit. Lesson: duration/time-range claims join the truth-debt grep class for future sweeps. |
| PH-12 | Scrub gate, first look at three same-day files, caught 30 hits: client-name references authored by the premium architect into two SHIPPING dispatch prompts (`L3_FIELD_EVIDENCE_ARCHITECT_PROMPT.md`, `USER_PAIN_RESEARCH_DISPATCH.md`) + operator home paths in a raw research file (`research/2026-07-11-user-pain-raw/1b-psp-firstrun.md`). | FIXED — redacted/genericized the two prompt files (client name → bucket + private-pointer phrasing), sanitized the research file's paths in place (operator home path → `~`). Gate performing as designed; allowlist untouched. Strengthens the day-2 watchlist item: a pre-commit/pre-push scrub hook should be prioritized so this class is caught at authoring time, not at build time. |
| PH-13 | Self-correction, same session: an earlier draft of this row claimed the dangling-reference check matches `path:line`-style citations only, and that a CHANGELOG.md entry naming the same excluded file (bare, no line number) escaped it as a live gap. That diagnosis was wrong. Reading `scripts/build-snapshot.sh`'s check directly shows a plain literal-substring match of an operator-held token list against the whole staged tree — no `path:line` sensitivity at all. The CHANGELOG.md entry passes because it's already covered by the pre-existing sed sanitize map (`launch/snapshot-sanitize.txt`), not because of any format limitation. The false diagnosis was itself caught the honest way: naming the excluded file to describe the (non-existent) gap re-tripped the same real check on the very next gate run. | FIXED — corrected before shipping; no checker gap exists, no gate-hardening follow-up needed. Optional follow-up, decision deferred: if a sanitized public home for the depth-ladder/self-over-confidence lesson is ever wanted, that's a post-launch DISPOSITIONS-class task. |

**Zero silent drops:** every finding above is FIXED here or DEFERRED with a named home.
