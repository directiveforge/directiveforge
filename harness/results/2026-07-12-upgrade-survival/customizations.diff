diff --git a/.claude/commands/update-context.md b/.claude/commands/update-context.md
index 1306dec..8d98ca7 100644
--- a/.claude/commands/update-context.md
+++ b/.claude/commands/update-context.md
@@ -26,6 +26,7 @@ Do NOT run after every commit — only after changes that make the context files
 5. Update `AGENTS.md` — patch stack summary, commands, or constraints if needed
 6. If an architectural decision was made: add one dated entry to `DECISIONS.md`
 7. If new dependencies with external services were added: run `/discover-mcp` for those deps only
+8. Update `docs/PRACTICE-METRICS.md` if session-tracking fields changed (owner-maintained metrics doc)
 
 ## Output
 
diff --git a/.claude/rules/base.md b/.claude/rules/base.md
index 37a641d..2a7826d 100644
--- a/.claude/rules/base.md
+++ b/.claude/rules/base.md
@@ -34,3 +34,11 @@ There is no test command — no test runner is installed.
 - NEVER add dependencies without confirming they don't already exist in `package.json`
 - NEVER skip the verify step — type errors and lint failures in committed code are unacceptable
 - NEVER reference paths that don't exist (no `src/`, no `app/api/`, no `middleware.ts`, no database) — the `@/` alias resolves to the project root
+
+## Tempo Deck house conventions (owner additions)
+
+- Practice-session fixtures live in `app/fixtures/` — extend `SEED_SESSIONS` there, never inline in pages
+- BPM values are integers 30–300; clamp at the component boundary, not in helpers
+- Never rename `PracticeSession` fields without updating the CSV export mapping
+- Prefer `TempoBadge` over raw BPM text in any list view
+- Commit messages: conventional prefixes, imperative mood
diff --git a/.claude/skills/git-hygiene/SKILL.md b/.claude/skills/git-hygiene/SKILL.md
new file mode 100644
index 0000000..dea9230
--- /dev/null
+++ b/.claude/skills/git-hygiene/SKILL.md
@@ -0,0 +1,26 @@
+---
+name: git-hygiene
+description: Keep Tempo Deck's git history clean — staged-only commits, conventional prefixes, no stray files.
+severity: MEDIUM
+confidence: high
+surface: claude-code
+---
+
+# Git Hygiene
+
+Owner-maintained skill — not generated by the kit.
+
+## Procedure
+
+1. Run `git status --porcelain` and review every entry before staging anything.
+2. Stage explicitly (`git add <path>`) — never `git add -A` while scratch files exist.
+3. Commit with a conventional prefix (`feat:` / `fix:` / `docs:` / `chore:`) in imperative mood.
+
+## Gotchas
+
+- `.env.local` must never appear in `git status` output as staged — abort if it does.
+- `next-env.d.ts` is generated; do not commit changes to it alone.
+
+## When NOT to use
+
+- Rebase/history-rewrite operations — out of scope for this skill.
diff --git a/CLAUDE.md b/CLAUDE.md
index 79494c7..3d2b4f2 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -28,7 +28,7 @@ Canonical command table lives in `AGENTS.md` — this section notes only quirks.
 - **Components**: `components/` — presentational, one component per file, composed top-down (`page` → `session-list` → `beat-counter` → `tempo-badge`).
 - **Helpers**: `lib/` — currently just `email.ts` (Resend stub).
 - **No API routes, no middleware, no data layer.** Do not assume `app/api/` or `middleware.ts` exist — they do not.
-- **Deploy**: Vercel (hobby), framework preset in `vercel.json`, region `iad1`.
+- **Deploy**: Vercel (hobby), framework preset in `vercel.json`, region `iad1`, custom domain tempodeck.app.
 
 ## Key Conventions
 
@@ -95,3 +95,8 @@ Working with an external service and struggling with its API:
 3. Quality-check (maintained? trusted? `uvx snyk-agent-scan@latest` passes? — needs `SNYK_TOKEN` for a full verdict).
 4. If it passes, add to `.mcp.json` + `.cursor/mcp.json` with a pinned version and a `.mcp.annotations.md` entry.
 5. Run `/discover-mcp` for a full dependency scan. Two vetted-but-not-installed candidates (Vercel RO, Resend) are documented in `.mcp.annotations.md`.
+
+## Local overrides (owner)
+
+- Weekend practice sessions get logged manually — see `docs/PRACTICE-METRICS.md`
+- Do not suggest a database until session count exceeds 500 (owner decision 2026-07-05)
