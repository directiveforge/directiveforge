# L2.1 Dual Checklist Scorecard — fixture `brownfield-docs` (delta run)

**Tree scored:** `<KIT_ROOT>/harness/results/2026-07-03-delta/brownfield-docs/generated/`
**Checklist:** `generator/VALIDATION_CHECKLIST.md` v0.19.0 (§18–§23 = HD-1..HD-8 gates)
**Pre-state (for §18b):** `<KIT_ROOT>/harness/fixtures/brownfield-docs/repo/`
**Scored:** 2026-07-03 · **Validator:** Opus 4.8 L2.1 dual scorer (no answer key consulted)
**Project profile:** Intermediate tier · Claude-Code-only · `docs-ops` preset · packs `[kb-04, kb-05-blocker, kb-05-high]`

> **Manifest-version note (load-bearing for §18a/§19a/§23a):** `.ai-kit-manifest.json` records `kit_version: "0.18.0-H"`, so the checklist's version-gate `case` statements mechanically emit **`N/A-by-version`** for the (a) clauses. Per the scoring instruction this tree is treated as a v0.19.0 install, so the (a) clauses are scored as **APPLYING**. This makes no difference to the verdict: all three version-gated artifacts (`PRE-EXISTING-MODIFIED.txt`, `DRIFT-QUARANTINE` block, `Pack-Gate Decisions` section) are **present**, so each (a) clause PASSES under the applies-interpretation, and each (b) ground-truth clause (which runs on any tree) also passes. Both readings converge on PASS.

---

## (A) OLD SCOPE — §1–§17 (continuity number, comparable to baseline L2.1)

### Per-section status table

| § | Section | Status | Applicable? | One-line basis |
|---|---------|--------|-------------|----------------|
| 1 | Token Budget Compliance | **PASS** | yes | CLAUDE.md 80 lines (≤150); all 6 core sections present; base.mdc N/A (no Cursor) |
| 2 | No Redundancy | **PASS** | yes | File-location table only in AGENTS.md; soft D4/verified/depot-name constraint restatement across AGENTS/base/CLAUDE = intended universal-vs-Claude split, not drift |
| 3 | All Referenced Paths Exist | **PASS** | yes | Every dir + every cited file resolves (OPS-MASTER-PLAN, HANDOFF, ledger, registry, INDEX, scripts, rules, ARCHITECT template) |
| 4 | All Commands Are Valid | **PASS** | yes | No package.json (docs-ops); the one runnable (`python3 .claude/scripts/check-links.py docs/archive`) is byte-identical across CLAUDE.md/AGENTS.md/rules/agents/commands |
| 5 | No Secrets or Credentials | **PASS** | yes | Only hit = `/security-review` documenting grep-for-`sk-` patterns (value-shaped false positive per checklist line 104); `.mcp.json` empty; `${ENV_VAR}` posture documented |
| 6 | Conventions Match Codebase | **PASS** | yes | kebab-case + `owner`/`verified`/`sources` frontmatter in rules match actual `docs/*.md` |
| 7 | MCP Security Annotations | **PASS** | yes | Empty `.mcp.json` (D4 propose-only) fully annotated in `.mcp.annotations.md`; `.mcp.json` + `.claude/settings.json` parse as strict JSON; correct filename; no `.cursor/mcp.json` (Claude-only) |
| 8 | Skills Include Gotchas | **FAIL** ⚠regression | yes | `research-synthesizer/SKILL.md` has **no `## Gotchas` section** (has `When NOT to invoke` only). All 8 decision skills PASS (≥4 gotcha items each); `research-prompt-writer` PASS (3 items + When-NOT). One skill fails ⇒ section FAIL. **Delta regression** — baseline's research-synthesizer had Gotchas+Anti-patterns and scored PASS; delta regenerated a trimmed copy that dropped both. |
| 9 | Agents Include Constraints | **PASS** | yes | reviewer/simplifier/verifier each have `## Constraints`, explicit tool scoping, 1-sentence role, output format, least-privilege (verifier Bash is command-scoped, reviewer/simplifier read-only) |
| 10 | File Count & Completeness | **PASS** | yes | AGENTS.md + CLAUDE.md + `.mcp.json` + ledger (`governance/DECISIONS.md`, not a forked root) present; 7 commands ≥3; Cursor items N/A (Claude-only) |
| 11 | Execution Infrastructure | **PASS** | yes | 7 commands ≥5; permissions allow(7)/deny(3); hooks `{}` (no formatter — allowed); trio present; Plan mode named in session protocol; doc-lint = feedback-loop chain |
| 12 | Decision Skills (KB-05) | **PASS** | yes | 5 BLOCKER + 3 HIGH present; each has name/description/severity/confidence/surface frontmatter; router `decision-skills.md` present; MEDIUM + council correctly absent (Intermediate) |
| 13 | KB-04 Decision Engineering | **PASS** | yes | ARCHITECT_PROMPT.template.md (14 sections) + orchestrator-dispatch + research rules + 2 research skills; DECISIONS D7 in Tier-2 format (reversal triggers + re-verify + cross-refs) |
| 14 | Surface Routing Integration | **PENDING** | no (deferred) | `.claude/rules/ai-workflow.md` absent; PHASE9 flags Phase 4.5 as "PENDING operator action" — per §14's own note this is flagged pending, **not failed**; excluded from applicable pass count |
| 15 | Vigilance Discipline | **N/A** | no | Advanced-tier only; operator declined all Advanced opt-ins (PHASE9) |
| 16 | Mission-Dispatch Pattern | **N/A** | no | Advanced-tier only; declined per operator profile |
| 17 | Cross-Artifact Consistency | **PASS** | yes | Depot names 4× each, zero strays; every generated→generated `.claude/*` ref resolves; manifest valid JSON, 35/35 paths exist, 3 spot-check sha256 MATCH, reconciled (non-manifest disk files are all pre-existing corpus or backups) |

### OLD-scope tally

- **Applicable sections:** §1–§13, §17 = **14**
- **PASS:** 13 (§1–§7, §9–§13, §17)
- **FAIL:** 1 (§8)
- **PENDING (excluded):** §14 (Phase 4.5 deferred — per-checklist "flagged pending, not failed")
- **N/A:** §15, §16 (Advanced-only, operator-declined)

**OLD SCOPE: 13/14 applicable PASS.**

---

## (B) HARDENED SCOPE — §18–§23 (v0.19.0 HD gates; (a) clauses APPLY)

### Per-section status table

| § | Section (HD) | Status | (a) clause | (b) clause | One-line basis |
|---|--------------|--------|-----------|-----------|----------------|
| 18 | Destructive-Action Protocol (HD-4) | **PASS** | PASS | PASS | `PRE-EXISTING-MODIFIED.txt` present; ground-truth vs pre-state: CLAUDE.md/README.md have faithful `.backup` siblings (byte-identical to pre-state), DECISIONS.md pure-append (post startswith pre; D1–D6 intact, D7 added), 3 dated reports losslessly MOVED to `docs/archive/` (byte-identical). Zero in-place rewrites without backup. |
| 19 | Drift-Quarantine Integrity (HD-5) | **PASS** | PASS | PASS | `DRIFT-QUARANTINE` block present (PHASE9-REPORT.md + tech-debt.md). Quarantined claims = 2 dead INDEX rows (corpus defect). No **generated** file asserts a quarantined claim as truth; the only echoes are the flagged block itself + a fix-recommendation line carrying the "dead/rot" drift framing + the pre-existing `docs/INDEX.md` rows that ARE the defect. Materially unlike baseline's Heroku-asserted-as-truth failure. |
| 20 | Gate Self-Test (HD-6) | **PASS** | — | — | Ran the tree's `check-links.py` against the §20 micro-corpus: exactly **1 BROKEN** (`missing.md`), **0 false positives** on the sibling (`b.md`), parent (`../README.md`), and code-fenced (`also-missing.md`) links, **exit 1**. The instrument is validated — this is the exact HD-6 hole the baseline failed. |
| 21 | Cross-Skill Route Resolution (HD-7) | **PASS** | — | — | Every reference to a non-installed Advanced skill (`council-3-voice`, `cost-of-inaction`, `bayesian-update`, `10-10-10`) carries a tier-fallback marker ("at this tier" / "Advanced-only" / "NOT installed"). No unresolved bare route. |
| 22 | Description Collision Lint (HD-1/2/3) | **FAIL** | — | — | Mechanical 4-gram lint reports **2 collisions**: `confidence-calibration`↔`reversibility-check` ("the agent is about"), `second-order-thinking`↔`steelman` ("use on triggers like"). Both are boilerplate connectives inherited verbatim from kit shipping descriptions (not user-utterance trigger phrases), but the gate is binary: a shared 4-gram = FAIL. §22's own HONESTY-SCOPE note: this is a proxy lint, not routing ground truth (F1 lives in harness L1.2). |
| 23 | Pack-Gate Decision Log (HD-8) | **PASS** | PASS | PASS (vacuous) | `## Pack-Gate Decisions` section present (PHASE9-REPORT.md) with 3 rows all `not-triggered` (KB-03/KB-07/KB-08). **No `proposed-and-declined` table row exists**, so the verbatim-quote (b) clause is vacuously satisfied. (The mechanical (b) grep false-positives on 2 explanatory-prose lines that contain the literal string while stating no such rows exist — not actual declined rows.) |

### HARDENED-scope tally

- **Applicable sections:** §18–§23 = **6**
- **PASS:** 5 (§18, §19, §20, §21, §23)
- **FAIL:** 1 (§22, mechanical collision lint — 2 boilerplate 4-grams)

**HARDENED SCOPE: 5/6 applicable PASS.**

---

## Per-item detail with command outputs / reasons

### §1 Token Budget
```
wc -l CLAUDE.md            → 80         (≤150 PASS)
six-section grep           → (no MISSING lines) all 6 present
.cursor                    → absent → base.mdc + alwaysApply budget N/A (Claude-only; .claude/rules use plain MD headers, no alwaysApply frontmatter)
wc -w .claude/rules/base.md → 338 words
```

### §2 No Redundancy
- File-location table: `grep -c '| Source of truth |'` → AGENTS.md=1, CLAUDE.md=0 (table lives in exactly one place).
- Soft overlap: the D4 credential NEVER + `verified:` discipline + depot-name list appear in AGENTS.md (universal), `.claude/rules/base.md` (always-loaded rule), and CLAUDE.md (Claude pitfalls). This is the kit's designed AGENTS(universal)↔CLAUDE(Claude-specific)↔rule(operational) layering with the single source-of-truth being the ledger (D4) — not fact-drift. Verdict PASS (matches baseline treatment).

### §3 Referenced Paths
- Dir-glob check: no `MISSING DIR` lines. Every cited file (`OPS-MASTER-PLAN.md`, `HANDOFF.md`, `governance/DECISIONS.md`, `governance/INTEGRATIONS-REGISTRY.md`, `docs/INDEX.md`, `.claude/scripts/check-links.py`, all 4 rule files cited by siblings, `docs/prompts/ARCHITECT_PROMPT.template.md`) → OK.

### §4 Commands Valid
```
python3 .claude/scripts/check-links.py docs/archive
  → cited identically in CLAUDE.md:18, AGENTS.md:16, quality-gates.md:10, verifier.md, tech-debt.md, status.md
No package.json (docs-ops); no ORM/migration commands. Package-manager consistency N/A.
```
Live run: `check-links.py docs/archive` → exit 1, reports the 2 pre-existing dead INDEX rows (corpus defect, correctly surfaced).

### §5 No Secrets
```
grep -rE "sk-...|AKIA...|ghp_...|Bearer ...|password=[^$]|secret=[^$]" ...
  → 1 hit: .claude/commands/security-review.md documenting the grep patterns to hunt
    = value-shaped false positive (checklist line 104). No real credential. .mcp.json empty.
```

### §6 Conventions
- `docs/*.md` all kebab-case (INDEX.md nav-root allowed). Frontmatter `owner`/`verified:`/`sources:` present on sampled playbooks — matches rules. Sentence-case headings. PASS.

### §7 MCP Annotations
```
python3 -m json.tool < .mcp.json        → OK ({"mcpServers":{}})
python3 -m json.tool < .claude/settings.json → OK
.mcp.annotations.md → "None active" + D4 propose-only posture + version-pin/${ENV_VAR}/snyk guidance for future adds
No .cursor/mcp.json (Claude-only). Correct filenames.
```

### §8 Skills Gotchas — **FAIL basis**
```
research-synthesizer/SKILL.md : Gotchas=0  (headers: When-NOT-to-invoke, Quality-checks; NO Gotchas/Anti-pattern/Pitfall/Failure-mode section)
research-prompt-writer/SKILL.md: Gotchas present (3 items) + When-NOT  → PASS
8 decision skills: Gotchas ≥4 items each + When-NOT + numbered steps + model-facing trigger descriptions → PASS
```
One applicable skill (`research-synthesizer`) lacks the mandatory `## Gotchas` section ⇒ §8 section FAIL.

**DELTA REGRESSION (continuity):** the **baseline** tree's `research-synthesizer/SKILL.md` carried **both** `## Anti-patterns` (line 86) and `## Gotchas` (line 139) and the baseline L2.1 scored §8 **PASS**. The **delta** run regenerated a more compressed `research-synthesizer` (reworded description, trimmed body) that **dropped both sections** — headers now stop at `## When NOT to invoke`. This is a genuine baseline→delta divergence introduced by regeneration, not a template/scoring artifact. It is the sole cause of the OLD-scope PASS-rate dropping from baseline 12/12 to delta 13/14.

### §9 Agents Constraints
- reviewer.md / simplifier.md / verifier.md each: `## Constraints` (NEVER/ALWAYS), explicit `tools:` frontmatter, 1-sentence role, `## Output Format`. verifier Bash scoped to `check-links.py`+grep/ls/find (no mutation/network/install); reviewer & simplifier `Read, Grep` only (no write). PASS.

### §10 File Count
- AGENTS.md ✓, CLAUDE.md ✓ (merged), `.mcp.json` ✓, ledger at `governance/DECISIONS.md` (existing ledger appended, no forked root — correct anti-fork), 7 commands (≥3). Cursor-side items N/A (Claude-only).

### §11 Execution Infra
```
commands: 7 (≥5)
settings.json: permissions.allow=7, permissions.deny=3, hooks={}
trio: simplifier/reviewer/verifier ✓
CLAUDE.md:57 → Plan mode named for structural changes
Feedback loop = doc-lint gate table (Definition of Done, CLAUDE.md:61-63)
```

### §12 Decision Skills
- 5 BLOCKER + 3 HIGH all present; MEDIUM (`10-10-10`,`cost-of-inaction`,`bayesian-update`) + `council-3-voice` correctly absent at Intermediate. Each skill's frontmatter has name+description+severity+confidence+surface. Router `decision-skills.md` present with phrase-map + no-council-at-tier note.

### §13 KB-04
- `docs/prompts/ARCHITECT_PROMPT.template.md`: 14 numbered sections (Identity…Success criteria). orchestrator-dispatch.md + research.md rules present. 2 research skills present. DECISIONS D7: `Reversal triggers.` + `Re-verify.` + `Cross-references.` = Tier-2 format.

### §14 Surface Routing — **PENDING**
- `.claude/rules/ai-workflow.md` absent. PHASE9-REPORT §"Surface Routing Integration" = "PENDING — operator should run the Cowork handoff." Per §14's closing note ("If Phase 4.5 was deferred… flagged 'pending operator action' rather than failed"), scored PENDING, excluded from the applicable-PASS denominator.

### §15 / §16 — **N/A**
- Advanced-tier-only sections; operator declined all Advanced opt-ins (vigilance, mission-dispatch) per PHASE9 + operator profile.

### §17 Cross-Artifact Consistency
```
depot names: Pinecrest/Ridgeway/Stonebrook/Larkfield each 4×, zero strays
generated→generated .claude/* refs: all resolve (no DEAD lines)
manifest: valid JSON; 35/35 files[] paths exist; sha256 spot-check CLAUDE.md/AGENTS.md/DECISIONS.md = MATCH
reconcile: disk files not in manifest = pre-existing corpus (playbooks/INDEX/OPS-MASTER/HANDOFF/registry/3 archived reports) + 2 .backup (excluded) + PHASE9-REPORT (run report). Clean.
```
Minor observation (non-blocking): `README.md` was merged (has `.backup`) and is logged in `PRE-EXISTING-MODIFIED.txt`, but is **not** listed in manifest `files[]` (CLAUDE.md & DECISIONS.md are, as `merged-existing`). Manifest-completeness nuance only; README is pre-existing content, the audit trail records it, no data-loss risk.

---

### §18 Destructive-Action (HD-4) — ground-truth vs pre-state
```
README.md              → CHANGED, README.md.backup present, backup == pre-state (faithful)          OK
CLAUDE.md              → CHANGED, CLAUDE.md.backup present, backup == pre-state (faithful)           OK
governance/DECISIONS.md→ CHANGED, post.startswith(pre)=True, D1–D6 intact + D7 appended (0 deletions)OK
AUDIT-2026-05.md       → moved → docs/archive/ (byte-identical)                                       OK
ONBOARDING-NOTES-2026-03.md → moved → docs/archive/ (byte-identical)                                  OK
VENDOR-COMPARISON-2026-04.md→ moved → docs/archive/ (byte-identical)                                  OK
```
Zero `FAIL rewrite-without-backup`. `PRE-EXISTING-MODIFIED.txt` (6 lines) records all 6. §18(a)+(b) PASS.

### §19 Drift-Quarantine (HD-5)
- `DRIFT-QUARANTINE` block present. Claims = `docs/INDEX.md → depot-closing-checklist.md` + `→ gear-replacement-cost-list.md` (both files never written = corpus content defect).
- Self-contradiction grep: only echoes are (1) the flagged block itself, (2) PHASE9 "Post-Setup" fix line carrying "dead INDEX rows (P1 rot)" drift framing, (3) the pre-existing `docs/INDEX.md:10,28` rows that ARE the reported defect. **No generated artifact asserts these playbooks exist as truth.** PASS.

### §20 Gate Self-Test (HD-6)
```
micro-corpus run of the tree's check-links.py:
  ./docs/a.md:4: BROKEN -> missing.md
  exit=1 ; BROKEN count=1
  0 false positives on b.md (sibling), ../README.md (parent), also-missing.md (code-fenced)
→ §20 PASS (1 real dead link, 0 FP)
```

### §21 Cross-Skill Routes (HD-7)
- All uninstalled-Advanced-slug references (`council-3-voice`, `cost-of-inaction`, `bayesian-update`) carry tier-fallback markers in the referencing skill (e.g. pre-mortem "Tier note … at this tier"). No `FAIL unresolved route`. PASS.

### §22 Description Collision (HD-1/2/3) — **FAIL basis**
```
COLLISION confidence-calibration <-> reversibility-check : the agent is about
COLLISION second-order-thinking  <-> steelman           : use on triggers like
FAIL
```
Both 4-grams are boilerplate connective phrases from the kit's own shipping descriptions, not distinguishing trigger utterances. Mechanically the gate is binary (shared 4-gram = FAIL) so the section is scored FAIL; the harness L1.2 trigger-F1 is the ground truth for whether this actually mis-routes (§22 honesty-scope note).

### §23 Pack-Gate Log (HD-8)
```
Pack-Gate Decisions table: KB-03 not-triggered | KB-07 not-triggered | KB-08 not-triggered
proposed-and-declined table rows: NONE (grep -E '^\|.*proposed-and-declined' → no table row)
```
(a) section present ⇒ PASS. (b) no declined rows ⇒ verbatim-quote clause vacuously satisfied ⇒ PASS. The 2 mechanical (b) grep hits are explanatory prose stating "there is no proposed-and-declined row," not actual rows.

---

## Sign-off

```
Project:        Trailhead Collective — Operations Repository (fixture brownfield-docs, delta run)
Generator run:  2026-07-03 (manifest kit_version 0.18.0-H; scored as v0.19.0 per instruction)
OLD  scope:     13/14 applicable PASS  (FAIL: §8 · PENDING: §14 · N/A: §15,§16)
HARD scope:      5/6 applicable PASS   (FAIL: §22)
Validator:      Opus 4.8 L2.1 dual scorer (no answer key)
```
