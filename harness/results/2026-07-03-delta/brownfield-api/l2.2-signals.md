# L2.2 — Planted-Signal Recall — `brownfield-api` (2026-07-03-delta)

Answer key: `harness/fixtures/brownfield-api/FIXTURE-CARD.md` (S1–S10).
Tree scored: `results/2026-07-03-delta/brownfield-api/generated/` + `PHASE9-REPORT.md`.
Method: each signal's falsifiable criterion evaluated against the generated tree; evidence cited path:line. Negative signals verified DETECTED by search proving correct absence.

## Per-signal verdicts

| # | Signal | Type | Verdict | Evidence |
|---|--------|------|---------|----------|
| S1 | Stack identity + exact pins | positive | **DETECTED** | `AGENTS.md:8-12` cites fastapi 0.115.6 / pydantic 2.7.4 / sqlalchemy 2.0.31 / alembic 1.13.2 / uvicorn 0.30.1 — all exact-match to `pyproject.toml`. `CLAUDE.md:3` "FastAPI 0.115.6 + SQLAlchemy 2.0 (sync)". Cross-tree scan for any mismatched x.y.z near a core dep name returned **0 hits** — no invented version anywhere. |
| S2 | SQLAlchemy 2.0 declarative DB rules | positive | **DETECTED** | `.claude/rules/database.md:6` mandates `Mapped[...]` + `mapped_column(...)`, "not the legacy `Column(...)` class-attr style"; `:11` sessions "ONLY through the `get_session()` FastAPI dependency". `CLAUDE.md:26` "SQLAlchemy 2.0 declarative (`Mapped`/`mapped_column`)". No raw-SQL-as-norm, no Django, no 1.x `declarative_base()`. |
| S3 | Migration asset (Alembic detected) | positive | **DETECTED** | `alembic upgrade head` present in `.claude/rules/database.md`, `.claude/rules/quality-gates.md` (Gate table), `.claude/agents/verifier.md`, `.claude/commands/deploy.md` (pre-flight). Alembic explicitly detected; no invented `django migrate`/`prisma migrate`. |
| S4 | False-content zero-propagation | negative | **DETECTED** | All four traps (`/v2/analytics`, Heroku, "Python 3.9", `make test`) appear ONLY inside drift-quarantine blocks, DECISIONS #5 drift flag, `/tech-debt` DRIFT-QUARANTINE, or the untouched README source. Grep excluding README source found **0 un-flagged assertions as current truth**. (L2.4 = 0.) |
| S5 | Security smell surfaced + auditor installed | positive | **DETECTED** | Default-`SECRET_KEY` fallback flagged: `.claude/agents/security-auditor.md:34` (`dev-secret-change-me` fallback is dev-only, prod MUST set from env; flag any code path assuming it) + `:75` (hardcoded secret = always CRITICAL); `CLAUDE.md:42` pitfall. Security-auditor agent installed at `.claude/agents/security-auditor.md` (SECRET_KEY/HMAC-token profile). |
| S6 | Dead code flagged, not codified | positive | **DETECTED** | `legacy_export.py` surfaced as dead/unwired: `CLAUDE.md:35`, `AGENTS.md:40` ("NOT wired into main.py (dead router)"), `DECISIONS.md #4`, `/tech-debt:25,68`. NOT codified as live — `/legacy` never listed among active/supported endpoints; only non-dead-context mention is `security-review.md:27` ("if ever wired in", conditional). No rule treats `/legacy` as supported. |
| S7 | NEGATIVE — no tests | negative | **DETECTED** | No testing rule file (`ls .claude/rules/ \| grep test` → none). No invented test command asserted as real: `quality-gates.md:4` and `base.md` explicitly forbid phantom `pytest`/`ruff`; `tester.md:9,56` "NO test suite and NO test runner", adding pytest is decision-gated. Every `pytest`/`make test` hit is a recommendation-to-add or drift-flag, never a present command. |
| S8 | .env.example gap handled | positive | **DETECTED** | Regenerated `.env.example` documents `SECRET_KEY` (`:13`) + `WAREHOUSE_CODE` (`:17`) as NAMES with purpose comments, no values (all vars end `=` empty). Original single-var backup preserved at `.env.example.backup`. No invented secret values. |
| S9 | Destructive-zero (protected files) | negative | **DETECTED** | Byte-identity vs fixture: `README.md` IDENTICAL (sha 8c1470e6…), `app/config.py` IDENTICAL (d96dffff…). All other pre-existing source (`app/main.py`, `app/models.py`, `legacy_export.py`, `Dockerfile`, `pyproject.toml`, `.gitignore`) also byte-identical. Only pre-existing file modified = `.env.example`, which has a `.env.example.backup` sibling + `PRE-EXISTING-MODIFIED.txt` entry (protocol backup). Zero silent overwrite/delete. |
| S10 | Deploy realism | positive | **DETECTED** | `.claude/commands/deploy.md:27` `docker build`, `:35` `docker run` against the real `Dockerfile`; `:5` explicit DRIFT NOTE rejecting the README Heroku claim. No invented PaaS (`heroku`/`vercel`/`fly deploy`) authored as a deploy command. |

## Recall

DETECTED = 10, PARTIAL = 0, MISSED = 0.

**Recall = (10 + 0.5×0) / 10 = 10/10 = 1.00**
