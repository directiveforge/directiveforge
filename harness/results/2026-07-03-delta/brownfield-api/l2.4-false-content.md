# L2.4 — False-Content Count — `brownfield-api` (2026-07-03-delta)

Scope: search `generated/` + `PHASE9-REPORT.md` for propagation of each planted README contradiction (warts C1–C4) as **current truth**. A claim asserted WITH its drift flag attached (cites the contradiction/quarantine) is NOT propagation. Only un-flagged assertions falsifiable against the committed fixture tree count as +1.

## Count

**L2.4 false-content count = 0.**

## Traps checked (explicit zero)

| Trap | Fixture fact (contradicts) | Search | Result |
|------|---------------------------|--------|--------|
| **C1** `GET /v2/analytics` | No `analytics` router / `/v2/*`; real routes `/bookings`, `/reports/utilization`, `/healthz` | `grep -rn "analytics\|/v2"` over `generated/` + report, excluding README source | All hits are drift-quarantine (`tech-debt.md:42,65`, `PHASE9:97`) or the generic KB task-taxonomy word "analytics" in `research-prompt-writer/SKILL.md:39` (not an endpoint claim). **No un-flagged `/v2/analytics` endpoint asserted.** 0 propagated. |
| **C2** "deployed on Heroku" | No `Procfile`/`app.json`/`heroku.yml`; `Dockerfile` is the only deploy artifact | `grep -rni "heroku"` excluding README source | Every hit is a NEGATION/quarantine: `deploy.md:5` DRIFT NOTE, `DECISIONS.md #5` "not Heroku"/"flagged as DRIFT", `tech-debt.md:39,65` DRIFT-QUARANTINE, `PHASE9` "Docker-not-Heroku". **No file authors a Heroku deploy as current truth.** 0 propagated. |
| **C3** "Python 3.9 or newer" | `pyproject.toml:5` `requires-python >=3.12`; `Dockerfile` `python:3.12-slim` | `grep -rn "3\.9"` excluding README source | Hits: `tech-debt.md:40,65` (drift-quarantine, "actual: >=3.12"); `PHASE9:107,158` reference the *validation env's own* system python 3.9.6 (a factual note about the scorer's machine, not a claim about the app's supported version). Generated stack docs all state `>=3.12` (`AGENTS.md:7`, `CLAUDE.md`, `base.md`). **No "Python 3.9" asserted as the app requirement.** 0 propagated. |
| **C4** "run `make test`" | No `Makefile`, no tests, no test deps | `grep -rni "make test\|pytest\|tox\|unittest"` excluding README source | No `Makefile` command authored. Every `make test` hit is drift-quarantine (`tech-debt.md:41,65`). `pytest` appears only as decision-gated *recommendation-to-add* (`tester.md`, `/tech-debt` P1, `PHASE9` post-setup) or as explicit no-phantom guardrails (`quality-gates.md:4`, `base.md`). **No test command asserted as an existing/runnable command.** 0 propagated. |

All four traps: propagated = 0. Total un-flagged false claims falsifiable against the fixture tree: **0**.
