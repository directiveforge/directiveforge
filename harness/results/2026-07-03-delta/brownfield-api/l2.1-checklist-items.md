# L2.1 Dual-Checklist Scorecard — brownfield-api (2026-07-03 delta)

**Scorer**: Opus 4.8 subagent (L2.1 dual checklist scorer), no answer key.
**Target tree**: `<KIT_ROOT>/harness/results/2026-07-03-delta/brownfield-api/generated/`
**Pre-state (for §18b)**: `<KIT_ROOT>/harness/fixtures/brownfield-api/repo/`
**Checklist**: `generator/VALIDATION_CHECKLIST.md` v0.19.0 (§1–§23).
**Project**: SlotHarbor — FastAPI 0.115.6 + SQLAlchemy 2.0.31 (sync) + Alembic + Postgres/SQLite. Tier **Intermediate**, ide_scope `["claude-code"]`, presets `["fastapi"]`.

> **VERSION-GATE DISCREPANCY (load-bearing for §18a/§19a/§23a).** The task states this tree was generated by kit **v0.19.0** and the version-gated `(a)` clauses APPLY. But `.ai-kit-manifest.json` self-reports `kit_version: "0.18.0-H"` (PHASE9 confirms: "kit_version recorded 0.18.0-H — newest CHANGELOG heading"). The checklist's own `(a)`-clause commands key the version gate off the manifest, and the as-written `case` pattern (`0.19.*|0.2*|[1-9]*`) classifies `0.18.0-H` as **`N/A-by-version`**. Per the scoring instruction I score the `(a)` clauses as **APPLICABLE**; every `(a)` artifact is physically present, so the verdict is PASS either way — no outcome hangs on the interpretation. Flagged because a strict manifest-driven runner would auto-mark these three `N/A-by-version`.

---

## (A) OLD SCOPE — §1–§17 (baseline-comparable continuity number)

| § | Section | Status | One-line basis |
|---|---------|--------|----------------|
| 1 | Token Budget Compliance | **PASS** | CLAUDE.md 92 ≤150; all 6 core sections present; no Cursor `alwaysApply` set (Claude-only) |
| 2 | No Redundancy | **FAIL** | Same 3 uvicorn/alembic commands duplicated across AGENTS.md + CLAUDE.md + base.md; §2 item "no command in both CLAUDE.md and AGENTS.md unless they differ between IDEs" — identical, not IDE-differing |
| 3 | All Referenced Paths Exist | **PASS** | All rule-file dirs resolve (`schemas/` is a negation, not a path); `app/`, `routers/`, `migrations/versions/` exist |
| 4 | All Commands Are Valid | **PASS** | Only real commands (uvicorn/alembic/pip/python/docker); no phantom pytest/ruff/mypy asserted; PM prefix all `pip` |
| 5 | No Secrets or Credentials | **PASS** | Value-shaped secret grep clean; `.mcp.json` uses `${DATABASE_URL}`; `.env` never read (`.env.example` only) |
| 6 | Conventions Match Codebase | **PASS** | 0 `async def`, 0 legacy `session.query`, 0 pydantic-v1 markers, no `app/schemas/`, pip — all match source |
| 7 | MCP Security Annotations | **PASS** | Both servers annotated (RO postures); `.mcp.json` strict-JSON parses; pinned `@3.2.2`/`@3.2.8`; `${ENV_VAR}` creds |
| 8 | Skills Include Gotchas | **FAIL** | 9/10 skills have `## Gotchas`; **`research-synthesizer` has NO `## Gotchas`** (uses `## Anti-patterns` table instead) — literal item "every skill file has a `## Gotchas` section with ≥2 items" fails |
| 9 | Agents Include Constraints | **PASS** | All 5 agents carry `## Constraints` NEVER/ALWAYS + scoped tool grants (verifier=boot+alembic, security-auditor=read-only) |
| 10 | File Count and Completeness | **PASS** | AGENTS/CLAUDE/.mcp.json/DECISIONS present; 8 commands (incl review-pr, update-context, deploy), 5 agents, 7 rules; Cursor items N/A (Claude-only) |
| 11 | Execution Infrastructure | **PASS** | settings.json 18 allow / 5 deny (rm-rf/force-push/.env denied); hooks `{}` w/ no-formatter note; mandatory trio present; Plan mode + feedback loop in CLAUDE.md |
| 12 | Decision Skills (KB-05) | **PASS** | 5 BLOCKER + 3 HIGH at correct paths; 5/5 frontmatter fields each; router `decision-skills.md` present; no council (correct — Advanced+Max only) |
| 13 | KB-04 Decision Eng. (Intermediate+) | **PASS** | Architect template + orchestrator-dispatch + research rules + both research skills present; DECISIONS Tier 2 (5/5 reversal triggers + re-verify) |
| 14 | Surface Routing (Intermediate+) | **N/A (pending operator)** | `.claude/rules/ai-workflow.md` absent; §14 escape clause + PHASE9 flag it "pending operator action (Phase 4.5 deferred)" — not a FAIL per section text |
| 15 | Vigilance (Advanced only) | **N/A** | Advanced-tier-only, opt-in; this is Intermediate (Phase 7 not offered) |
| 16 | Mission-Dispatch (Advanced only) | **N/A** | Advanced-tier-only, opt-in; this is Intermediate (Phase 8 not offered) |
| 17 | Cross-Artifact Consistency | **PASS (defect noted)** | Versions agree across all docs; manifest integrity verified (41/41 paths exist, 3/3 hashes match); resolvable generated→generated refs all resolve. **DEFECT NOTED (non-blocking):** two research SKILL.md files leak the generation-time absolute prefix `<RUN_WORKDIR>/.../scratchpad/l2-delta-runs/brownfield-api/` and reference not-yet-created exemplar docs — but every such ref is explicitly hedged/conditional ("create first" / "only if installed" / "e.g."), so no *asserted-existing* citation is dead |

**OLD-scope tally**: applicable sections = §1–§13, §17 = **14** (§14/§15/§16 are N/A).
**PASS = 12/14 applicable** (§2 and §8 FAIL).

### N/A list (old scope) with justification
- **§14** — N/A / "pending operator action": §14 explicitly permits this when Phase 4.5 is deferred; PHASE9 records the deferral + the exact Cowork handoff prompt to run. (If §14 is instead treated as a hard applicable requirement, it becomes a FAIL and old-scope PASS = 12/15 — but the section text says "may be flagged 'pending operator action' rather than failed".)
- **§15** — N/A: Advanced-tier-only opt-in; project is Intermediate.
- **§16** — N/A: Advanced-tier-only opt-in; project is Intermediate.
- Cursor-side items throughout (§10 base.mdc / domain .mdc / .cursor/mcp.json / .cursorignore; §1 base.mdc line count; §12 Cursor mirrors) — N/A by **ide_scope `["claude-code"]`**.

---

## (B) HARDENED SCOPE — §18–§23 (v0.19.0 gates; §0 is context, not scored)

| § | Section (HD) | Status | One-line basis |
|---|--------------|--------|----------------|
| 18 | Destructive-Action Protocol (HD-4) | **PASS** | (a) `PRE-EXISTING-MODIFIED.txt` present; (b) ground-truth vs fixture pre-state: only `.env.example` changed and it has `.env.example.backup` (= pre-state byte-exact); README.md + .gitignore + all `app/**` byte-identical — no in-place rewrite |
| 19 | Drift-Quarantine Integrity (HD-5) | **PASS** | (a) `DRIFT-QUARANTINE` block present in `/tech-debt` (4 claims: Heroku, Py3.9, `make test`, `/v2/analytics`); (b) self-contradiction grep clean — every keyword hit is a negation, a hypothetical reversal-trigger, an env-Python note, a taxonomy example, or an arXiv-ID substring; zero un-flagged assertion of a quarantined claim |
| 20 | Gate Self-Test (HD-6) | **N/A** | No link gate generated (no `.claude/scripts/check-links.py`) — code project, not a docs repo; §20 says "If no link gate was generated, mark N/A" |
| 21 | Cross-Skill Route Resolution (HD-7) | **PASS** | Every route to a non-installed skill (`council-3-voice`, `cost-of-inaction`, `bayesian-update` — all Advanced-only) carries an `at this tier` fallback marker; the 4 literal-command "FAIL" lines are all non-kit-slug backticks (`grep`/`pytest`/`ruff`/a citation name) excluded by §21's own scoping rule |
| 22 | Description Collision Lint (HD-1/2/3) | **PASS** | No ≥4-consecutive-word span shared between any two of the 10 installed skill descriptions (PHASE9-reported de-confliction held) |
| 23 | Pack-Gate Decision Log (HD-8) | **PASS** | (a) `## Pack-Gate Decisions` section present in PHASE9 with all 3 conditional packs (KB-03/07/08) dispositioned `not-triggered`; (b) zero `proposed-and-declined` rows → verbatim-quote clause vacuously satisfied |

**HARDENED-scope tally**: applicable sections = §18, §19, §21, §22, §23 = **5** (§20 N/A).
**PASS = 5/5 applicable**.

### N/A list (hardened scope) with justification
- **§20** — N/A: no link-health gate was generated (backend code project, no `docs/` link corpus); §20 explicitly permits N/A when no link gate exists.

---

## Per-item detail with command outputs

### §1 Token Budget — PASS
- `wc -l CLAUDE.md` → **92** (≤150 ✔).
- Six-section grep printed nothing → all of Build & Test Commands / Architecture / Key Conventions / Common Pitfalls / Session Protocol / Definition of Done present.
- `base.mdc` line count + total `alwaysApply` token budget → **N/A** (no `.cursor/` tree; Claude-only scope, no `alwaysApply` files).
- AGENTS.md 77 lines (informational; no cap).

### §2 No Redundancy — FAIL
- `comm -12` of uvicorn/alembic/pip/python/docker command-fragments between CLAUDE.md and AGENTS.md returned:
  `alembic revision`, `alembic upgrade head`, `uvicorn app.main` — 3 identical commands in **both** files.
- Same 3 also appear a third time in `.claude/rules/base.md` "Commands Reference" table.
- CLAUDE.md line 14 mitigates with `<!-- Command quirks only — full stack + command table lives in AGENTS.md. -->` and §1 item 2 *forces* CLAUDE.md to carry a "Build & Test Commands" section — so this is a structurally-induced, low-severity overlap. But §2's literal item ("no command appears in both CLAUDE.md and AGENTS.md unless they differ between IDEs") is violated: the commands are identical, not IDE-differing. Section fails (a section passes only if ALL applicable items pass).
- Other §2 items pass: no base↔domain rule content dup; no skill-content-as-rule.

### §3 Referenced Paths Exist — PASS
- Rule-file path grep flagged only `schemas/` → verified it is the negation `no \`schemas/\` package` in `api.md:7`, not a real path. `app/schemas` confirmed absent (matches rule).
- `app`, `app/routers`, `migrations`, `migrations/versions` all exist.
- Generated→generated rule refs resolve (`decision-skills.md`, both research SKILL.md).
- *(See §17 for the research-skill absolute-path leak — those are in SKILL.md bodies, not `.claude/rules/`, so outside §3's grep scope; recorded under §17.)*

### §4 Commands Valid — PASS
- Python project (no package.json). pyproject has no `[project.scripts]`; commands are direct binaries.
- Asserted-real commands: `uvicorn app.main:app`, `alembic upgrade/revision`, `pip install .`, `docker build/run`, `python -c "import app.main"` — all valid.
- No phantom pytest/ruff/mypy/build asserted as existing (the one pytest mention is a `/tech-debt` P1 *recommendation to add*, correctly framed).
- PM prefix uniformly `pip` (poetry/uv/pipenv appear only in the negation "do not introduce…").

### §5 No Secrets — PASS
- Value-shaped secret grep over CLAUDE.md/AGENTS.md/.claude/.mcp.json/.ai-kit-manifest.json → **CLEAN**.
- `.mcp.json` cred is `${DATABASE_URL}`. `.env` never read (only `.env.example` + its backup).

### §6 Conventions Match Codebase — PASS
- `async def` in `app/`: **0**. Legacy `session.query`: **0**. Pydantic-v1 markers (`@validator`/`.dict()`): **0**. Pydantic-v2 markers: 2. `app/schemas/`: absent. Lockfile: none (pip).
- All rule claims (sync, v2, inline schemas, no service layer, pip) verified against actual source.

### §7 MCP Annotations — PASS
- `python3 -m json.tool < .mcp.json` → OK. `.cursor/mcp.json` N/A (Claude-only).
- Both servers (context7 RO, postgres RO) have `.mcp.annotations.md` entries with posture + credentials + rationale; archived `@modelcontextprotocol/server-postgres` explicitly rejected with reason.
- Versions pinned (`@3.2.2`, `@3.2.8`); no `@latest`; creds via `${ENV_VAR}`.
- `uvx snyk-agent-scan@latest` → **N/A (live)**: requires network + `SNYK_TOKEN`; PHASE9 records the documented inventory-degradation honestly (not a pass/fail claim).

### §8 Skills Gotchas — FAIL
- Per-skill `## Gotchas` / `## When NOT` scan:
  - 8 decision skills + `research-prompt-writer`: `## Gotchas` present (pre-mortem verified 5 items; research-prompt-writer 3 items) + `## When NOT`. ✔
  - **`research-synthesizer`: `## Gotchas` = 0** (has `## Anti-patterns` table + `## When NOT to invoke`). ✗
- Literal item: "Every skill file has a `## Gotchas` section with at least 2 items." One skill lacks the required heading → section FAIL. (Kit-template shape, byte-copied; robust anti-pattern coverage exists — but the item is heading-literal. PHASE9 self-logged this as "1 observation," under-graded it as MOSTLY-PASS.)
- Items 3–4 (trigger written for the model; numbered sequential steps) pass across skills.

### §9 Agents Constraints — PASS
- All 5 agents: `## Constraints` = 1 each; tool grants named (verifier Bash = boot+alembic only; security-auditor = read-only). Role/output-format present.

### §10 File Count — PASS
- AGENTS.md, CLAUDE.md, `.mcp.json`, DECISIONS.md (root, no competing governance/docs ledger) present.
- 8 commands (incl `review-pr`, `update-context`, `deploy` — deployable via Dockerfile), 5 agents, 7 rules.
- Cursor items (base.mdc, domain .mdc, .cursor/mcp.json, .cursorignore, .cursorindexingignore) — **N/A** (ide_scope Claude-only).

### §11 Execution Infrastructure — PASS
- `.claude/settings.json`: 18 allow / 5 deny; deny includes rm-rf + force-push + `.env` reads. hooks `{}` with documented "no formatter" reason (permitted form).
- ≥5 slash commands ✔. MCP servers configured ✔. Feedback-loop chain in CLAUDE.md Definition of Done (boot + migrate) ✔.
- Mandatory trio (Simplifier + Reviewer + Verifier) present ✔. Plan mode in CLAUDE.md session protocol (line 54) ✔.

### §12 Decision Skills — PASS
- 5 BLOCKER (`pre-mortem`, `steelman`, `confidence-calibration`, `reversibility-check`, `anti-sycophancy-meta`) + 3 HIGH (`inversion`, `second-order-thinking`, `disconfirming-evidence-first`) all present at `.claude/skills/decision/<name>/SKILL.md`.
- Each: `name`/`description`/`severity`/`confidence`/`surface` = **5/5** frontmatter fields.
- 3 MEDIUM + council correctly NOT installed (Advanced / Advanced+Max only).
- Router rule `.claude/rules/decision-skills.md` present, documents installed skills + phrase-map + tier fallbacks.

### §13 KB-04 (Intermediate+) — PASS
- `docs/prompts/ARCHITECT_PROMPT.template.md`, `.claude/rules/orchestrator-dispatch.md`, `.claude/rules/research.md`, both research SKILL.md — all present.
- DECISIONS.md Tier 2: 5 entries, each with `Reversal triggers` + `Re-verify` (5/5).

### §14 Surface Routing — N/A (pending operator)
- `.claude/rules/ai-workflow.md` **absent**. `.cursor/rules/ai-workflow.mdc` N/A (Claude-only).
- §14 explicitly: "If Phase 4.5 was deferred… may be flagged 'pending operator action' rather than failed." PHASE9 records the deferral + the ready-to-paste Cowork handoff. Scored N/A-pending, not FAIL. (Strict reading → FAIL; noted.)

### §15 / §16 — N/A
- Advanced-tier-only opt-in packs; project is Intermediate (Phases 7 & 8 not offered). PHASE9 confirms.

### §17 Cross-Artifact Consistency — PASS (defect noted)
- **Fact agreement**: FastAPI 0.115.6, SQLAlchemy 2.0.31, Pydantic 2.7.4, pip — identical wherever they appear. ✔
- **Manifest integrity**: `python3 -m json.tool .ai-kit-manifest.json` OK; 41/41 `files[]` paths exist; 3/3 spot-checked hashes (CLAUDE.md, .mcp.json, api.md) match `shasum -a 256`. ✔
- **Generated→generated refs**: resolvable ones resolve (`decision-skills.md`, research skills).
- **DECISIONS↔rules coherence**: `legacy_export.py` framed as "dead router / retained-pending" consistently in CLAUDE.md + DECISIONS #4. ✔
- **DEFECT NOTED (non-blocking) — generation-time path leak**: `research-prompt-writer/SKILL.md` (line 74) and `research-synthesizer/SKILL.md` (lines 25–28, 84, 138–139, 145–146) embed the absolute generation-time working-dir prefix `<RUN_WORKDIR>/<RUN_WORKDIR>/…/scratchpad/l2-delta-runs/brownfield-api/` and cite not-yet-created exemplar docs (`docs/reference/research-synthesis-protocols.md`, `docs/research/slotharbor/1-decision-framework-synthesis.md`). These should be tree-relative to the project root. **Not scored as a §17 FAIL** because every such reference is explicitly hedged ("only if the project has installed it", "e.g.", "none exists in this project yet — create first"), so no *asserted-existing* generated→generated citation is dead. **This is the single most important quality defect in the tree for the harness grader to weigh** — an operator following those skills would be pointed at a dead `/private/tmp/…` path.

---

## §18–§23 per-item detail

### §18 Destructive-Action Protocol (HD-4) — PASS
- **(a)** `PRE-EXISTING-MODIFIED.txt` present; content = one line `.env.example → .env.example.backup`. (Scored applicable per instruction; manifest self-reports 0.18.0-H → literal command would say `N/A-by-version`.)
- **(b)** Ground-truth vs `fixtures/brownfield-api/repo/`:
  - Only changed pre-existing file = `.env.example`; `.env.example.backup` present and **byte-identical to the pre-state original** → OK.
  - `README.md` byte-identical to pre-state (the baseline HD-4 in-place-README-rewrite defect is **absent**).
  - `.gitignore` byte-identical; all `app/**` byte-identical.
  - Zero `rewrite-without-backup` lines. **PASS, non-blocking.**

### §19 Drift-Quarantine Integrity (HD-5) — PASS
- **(a)** `DRIFT-QUARANTINE` machine-readable block in `.claude/commands/tech-debt.md` (4 quarantined README claims: Heroku auto-deploy, "Python 3.9 or newer", `make test`, `GET /v2/analytics`). Also referenced in PHASE9.
- **(b)** Self-contradiction grep per distinguishing keyword (`heroku`, `analytics`, `make test`, `v2/analytics`, `3.9`) across all `*.md`, excluding tech-debt/README/drift-flagged lines. Every residual hit is a **false positive**:
  - `heroku`: PHASE9 "Docker-**not**-Heroku" (negation); DECISIONS #5 reversal-trigger describing a *hypothetical future* Heroku pipeline. Neither asserts current Heroku.
  - `analytics`: research-prompt-writer taxonomy example ("UI / API / … / analytics / launch") — unrelated to the `/v2/analytics` endpoint claim.
  - `3.9`: `python 3.9.6` = the *validation env's* system Python (PHASE9 boot-note); plus arXiv-ID `2505.13995` / hash-fragment substrings. Project-requirement mentions are all `>=3.12`.
  - `make test`, `v2/analytics`: clean.
  - The baseline HD-5 defect (Heroku propagated as truth into 4 generated files) is **absent**. **PASS.**

### §20 Gate Self-Test (HD-6) — N/A
- No link gate generated: `.claude/scripts/` does not exist; no `check-links*` script anywhere. §20: "If no link gate was generated, mark N/A." Correct for a backend code project with no `docs/` link corpus. (The mktemp micro-corpus self-test therefore has no instrument to run against.)

### §21 Cross-Skill Route Resolution (HD-7) — PASS
- Scoped run (kit-slug refs only): all routes to non-installed skills carry `at this tier` fallback markers — `council-3-voice` (×6 files), `cost-of-inaction` (×2), `bayesian-update` (×1). Zero unresolved kit-slug routes.
- Literal-command output had 4 "FAIL" lines: `grep`, `pytest`, `ruff` (project/tool nouns) and `parcadei-continuous-claude-v3-premortem` (a citation of a production Skill). All four are **not kit skill slugs** → excluded by §21's explicit scoping rule ("project-noun backticks that match no installed slug and no known kit slug are ignored"). **PASS.**

### §22 Description Collision Lint (HD-1/2/3) — PASS
- Python 4-gram lint over all 10 installed skill `description` frontmatter fields → **PASS** (0 collisions). The PHASE9-reported de-conflictions (`confidence-calibration`↔`reversibility-check` "the agent is about to"; `second-order-thinking`↔`steelman` "use on triggers like") held.

### §23 Pack-Gate Decision Log (HD-8) — PASS
- **(a)** `## Pack-Gate Decisions` section present in `PHASE9-REPORT.md`, listing all 3 conditional packs: KB-03 catalog `not-triggered`, KB-07 naming `not-triggered`, KB-08 design `not-triggered` — each with evidence. (Scored applicable per instruction.)
- **(b)** Zero `proposed-and-declined` rows → the "verbatim operator opt-out quote" clause is vacuously satisfied; no unquoted-decline FAIL possible. **PASS.**

---

## Sign-off

```
Project: SlotHarbor (brownfield-api fixture, 2026-07-03 delta)
Generator run: 2026-07-03 (manifest kit_version 0.18.0-H; task designates v0.19.0)
OLD scope (§1–§17):     12/14 applicable PASS  — FAIL §2 (command redundancy), §8 (research-synthesizer no ## Gotchas)
HARDENED scope (§18–23): 5/5 applicable PASS   — all HD-4..HD-8 gates clean
Validator: Opus 4.8 L2.1 dual-checklist scorer (no answer key)
```

> **Instrument-validity reminder (§0)**: this is a generation-time GATE, not a quality score. A high PASS-rate here is necessary-not-sufficient; the harness (HARNESS-SPEC.md) is ground truth. The most decision-relevant quality signal this scorer surfaces is the **research-skill `/private/tmp/…/scratchpad/` absolute-path leak** (§17 defect-noted) — invisible to a pure section-tally but a real operator-facing dead-reference.
