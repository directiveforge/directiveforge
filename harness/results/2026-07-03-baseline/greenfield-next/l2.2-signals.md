# L2.2 — Planted-Signal Recall — greenfield-next

**Run**: 2026-07-03-baseline · **Fixture**: greenfield-next (Tempo Deck) · **Scorer**: Opus 4.8 answer-key subagent
**Tree scored**: `results/2026-07-03-baseline/greenfield-next/generated/`
**Answer key**: `fixtures/greenfield-next/FIXTURE-CARD.md` §Answer key (frozen at fixture commit)

Verdict rule: DETECTED / MISSED (card defines no PARTIAL band for any of S1–S10). Negative signals (S4, S5) are DETECTED when the artifact is correctly ABSENT, verified by search. Recall = (DETECTED + 0.5×PARTIAL) / 10.

## Per-signal table

| ID | Type | Verdict | Evidence (path:line) + reasoning |
|----|------|---------|----------------------------------|
| **S1** | positive | **DETECTED** | Both files name Next.js + App Router + `15.1`. `CLAUDE.md:3` "Next.js 15.1 (App Router)"; `CLAUDE.md:26` "`app/` (App Router)". `AGENTS.md:8` "Next.js 15.1.6 (App Router)"; `AGENTS.md:40` "App Router with server components". No `14.x`/`15.3`/Pages-Router claim anywhere (grep for `14.[0-9]\|15.3\|pages router` → 0 hits). |
| **S2** | positive | **DETECTED** | Every install/run verb is `npm`. Command scan across CLAUDE.md/AGENTS.md/rules resolves only to `npm run dev/build/lint/start` + `npx tsc`/`npx vercel`. Zero `pnpm`/`yarn`/`bun` **as a command verb**: the only two hits are negation statements — `.claude/rules/base.md:15` "NEVER introduce yarn/pnpm/bun" and `PHASE9-REPORT.md:85` "no yarn/pnpm/bun mixing" — neither is an invocation. |
| **S3** | positive (design pack) | **MISSED** | Criterion requires the design pack PRESENT: `.claude/skills/design/` with 4 skills (`design-architect`, `design-engineer`, `design-reviewer`, `elevation-workflow`) **and** `docs/design/DESIGN-SPINE.md`. All absent: `.claude/skills/` contains only `decision/` (verified `ls .claude/skills/` → `decision` only); `find … -path "*skills/design*" -o -name DESIGN-SPINE.md` → 0 results; no `docs/` dir exists. The generator logged this as a deliberate deviation (`PHASE9-REPORT.md:151` — "literal gate met, deliberately NOT installed"), but the card's falsifiable criterion scores on presence, not on the justification. FAIL condition ("design pack absent") is met → MISSED. |
| **S4** | NEGATIVE (no tests) | **DETECTED** | Correctly absent, verified by search. No testing rule file: `find … -iname "*testing*"` → 0 rule files (only `.cursor/rules/testing.mdc` is named in `PHASE9-REPORT.md:46` as NOT generated). No `test` command string introduced: every "test" hit is a negation or the tester-agent refusing to fabricate one (`CLAUDE.md:16`, `AGENTS.md:26,48`, `.claude/rules/quality-gates.md:12`, `.claude/agents/verifier.md:45` "do not invent `npm test`", `.claude/agents/tester.md` "no test framework installed"). No `npm test`/`vitest`/`jest`/`playwright test` used as a live command. |
| **S5** | NEGATIVE (no database) | **DETECTED** | Correctly absent, verified by search. No `database.mdc`/DB rule (`find … -iname "*database*"` → 0). No migration skill/command (`.cursor/skills/migration/` not generated per `PHASE9-REPORT.md:47`). Every `prisma`/`drizzle`/`migrate`/`migration` token is either a negation (`AGENTS.md:36` "no `prisma/`", `AGENTS.md:26` "no `migrate` … script") or a generic worked-example inside a decision skill (`skills/decision/pre-mortem/SKILL.md:28,62` SSO/schema-migration analogy; `reversibility-check/SKILL.md:83`) — none is a DB rule/skill/command for this project. |
| **S6** | positive (deployable) | **DETECTED** | Deploy affordance generated referencing Vercel: `.claude/commands/deploy.md:1-52` (pre-flight gates + `npx vercel --prod` + Vercel rollback) and `.cursor/skills/deployment/SKILL.md` (both grep-match "vercel"). `vercel.json:3` `"framework": "nextjs"` is the deployability evidence. |
| **S7** | positive (conventions) | **DETECTED** | Rules state named exports + kebab-case explicitly. Named exports: `CLAUDE.md:20`, `AGENTS.md:32`, `.cursor/rules/base.mdc:21`, `.cursor/rules/frontend.mdc:11` (route entries correctly noted as also `export default` per framework). Kebab-case files: `.claude/rules/base.md:17`, `CLAUDE.md:22`, `.cursor/rules/base.mdc:22`. No rule mandates default exports or PascalCase/camelCase **file** naming (PascalCase is correctly scoped to component identifiers, not filenames). |
| **S8** | positive (strict TS) | **DETECTED** | Strict mode asserted in CLAUDE.md and rules. `CLAUDE.md:3` "TypeScript strict"; `CLAUDE.md:15` "tsconfig is `strict: true`"; `.claude/rules/base.md:18` "strict mode (`tsconfig.json`) — no `any`"; `.cursor/rules/base.mdc:18` "TypeScript 5 (strict)". No rule relaxes/disables strict. |
| **S9** | positive (env integrity) | **DETECTED** | `.env.example` NOT deleted and NOT overwritten — `diff` against `fixtures/greenfield-next/repo/.env.example` is **byte-identical** (both original vars + purpose comments survive). No new env var names invented: the only names referenced in generated docs (`RESEND_API_KEY`, `NEXT_PUBLIC_APP_NAME`) are each read in code — `lib/email.ts:7` `process.env.RESEND_API_KEY`, `app/page.tsx:9` `process.env.NEXT_PUBLIC_APP_NAME`. |
| **S10** | positive (no phantom cmds) | **DETECTED** | Every command string resolves. Full scan of CLAUDE.md/AGENTS.md/rules yields only: `npm run dev/build/lint/start` (all in `package.json:5-9` scripts), `npx tsc --noEmit` (via `typescript` dep), `next lint`/`next build` (via `next` dep), `npx vercel` (via `vercel.json`). Invented-command probe (`npm run typecheck\|format\|test`, `prettier`, `npm run seed\|migrate`) → **0 hits**. The docs explicitly warn there is *no* `typecheck`/`test` script and use `npx tsc --noEmit` instead (`CLAUDE.md:15`, `AGENTS.md:26`). |

## Recall

DETECTED = 9 · MISSED = 1 (S3) · PARTIAL = 0

**Recall = (9 + 0.5×0) / 10 = 9/10 = 0.90**

### Note on the single miss (S3)
S3 is the only miss and it is a *judgment* miss, not a hallucination: the target genuinely has the design surface the signal keys on (customized `tailwind.config.ts` tokens — `beat`/`stage`/`accent`/`display`/`gutter`/`card` all present in the fixture), and the generator explicitly recognized the literal gate fired then chose not to install the pack, citing operator "basics"/all-opt-ins-"no" intent (`PHASE9-REPORT.md:151`). The card's criterion is presence-based, so it scores MISSED. This is a defensible operator-intent call flagged for the L2 protocol's adjudication layer, not a correctness defect.
