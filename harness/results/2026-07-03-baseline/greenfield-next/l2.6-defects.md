# L2.6 — Independent Adversarial Workflow-Defect Review — greenfield-next

- **Fixture:** greenfield-next (Tempo Deck — Next.js 15.1.6 App Router, Starter tier, Claude Code + Cursor)
- **Run:** 2026-07-03-baseline
- **Reviewer:** Opus 4.8, independent step-④ falsification pass, fresh context, no answer-key access (FIXTURE-CARD not read)
- **Inputs:** generated tree vs pristine source (`fixtures/greenfield-next/repo/`)
- **Taxonomy:** HARNESS-SPEC §5 L2.6 classes (a)–(h); one primary class per defect; severity 🚨/⚠/🟡; verbatim evidence + file:line mandatory.

## Summary counts

| | Count |
|---|---|
| **Total findings** | 3 |
| 🚨 blocker | 0 |
| ⚠ major | 1 |
| 🟡 minor | 2 |

Per class: (a) phantom-reference **1** · (b) contradiction **0** · (c) dead-asset **0** · (d) stale-content **1** · (e) scope-misroute **0** · (f) governance-violation **0** · (g) placeholder-leak **1** · (h) duplication-drift **0**.

---

## Findings

### (a) phantom-reference — 1 finding

| # | Sev | File:line | Defect | Evidence |
|---|-----|-----------|--------|----------|
| A1 | ⚠ | `.claude/skills/decision/anti-sycophancy-meta/SKILL.md:43` (and 10 further hits in this file; systemic across all 5 installed skills) | The decision-skill pack ships byte-identical to the kit's **full-catalog** templates, so every installed Starter skill routes to sibling skills that are **not installed at this tier**. `anti-sycophancy-meta` is the auto-fire-FIRST BLOCKER: its step-3 routing table and its worked example send "finished artifact" ownership signals to `disconfirming-evidence-first`, which does not exist here (Starter set = pre-mortem, steelman, confidence-calibration, reversibility-check, anti-sycophancy-meta). The router rule itself confirms `disconfirming-evidence-first` is an **Intermediate** graduation add. So the highest-priority skill's primary route dead-ends. `reversibility-check` is worst-hit (14 refs to non-installed `council-3-voice` / `disconfirming-evidence-first` / `inversion`). | SKILL.md:43 `**Finished artifact (email, draft, code, copy, design)** → \`disconfirming-evidence-first\` (one-sided search…)`; worked-example output SKILL.md:70/93 `Routed to: disconfirming-evidence-first.` vs `.claude/rules/decision-skills.md:40` `Graduation: at Intermediate tier, add the 3 HIGH skills (inversion / second-order-thinking / disconfirming-evidence-first)`. `ls .claude/skills/decision/` → only the 5 Starter skills present. |

*Scope note:* primary class is (a) — the routes point at packages/skills that do not exist in the installed set. It has a scope-misroute (e) flavor (full-catalog template installed against a 5-skill subset without route pruning), but per the one-primary-class rule it is filed as phantom-reference. The `council-3-voice` mentions are hedged as cost-gated and reachable only via `reversibility-check` (which IS installed), so they are less acute than the flat `disconfirming-evidence-first` route; still non-existent here.

### (d) stale-content — 1 finding

| # | Sev | File:line | Defect | Evidence |
|---|-----|-----------|--------|----------|
| D1 | 🟡 | `PHASE9-REPORT.md:15` (also :24) | Self-report states AGENTS.md is 74 lines; the file is 73 lines (`awk 'END{print NR}' AGENTS.md` → 73). Off-by-one line-count claim in the generator's own report. Cosmetic — the ≤-ceiling verdict is unaffected — but it is a claim contradicting the shipped file. | `PHASE9-REPORT.md:15` `\`AGENTS.md\` — 74 lines (canonical stack + command table…)`; actual `AGENTS.md` = 73 lines. |

### (g) placeholder-leak — 1 finding

| # | Sev | File:line | Defect | Evidence |
|---|-----|-----------|--------|----------|
| G1 | 🟡 | `HANDOFF.md:1` (18 distinct `{{…}}` tokens across the file) | Root-level shipped `HANDOFF.md` is the raw kit template — 18 unresolved `{{…}}` placeholders, including `{{PROJECT_NAME}}` in the H1. It is the ONLY non-template file with `{{…}}` residue. **Mitigating context (keeps it 🟡 not 🚨):** manifest records `sha256 == template_sha256` (deliberately unfilled), PHASE9-REPORT:18 documents it as "template only, not filled … a session-state slot", and `/status` (`.claude/commands/status.md`) is wired to populate the machine section on first run. It is an intentional ephemeral slot, not a botched substitution — but by the letter of class (g), a shipped root file carrying `# Session Handoff — {{PROJECT_NAME}}` is placeholder residue in non-template output. | `HANDOFF.md:1` `# Session Handoff — {{PROJECT_NAME}}`; body carries `{{COMPLETED_TASK_1}}`, `{{FILE_1}}`, `{{TYPECHECK_STATUS}}`, `{{ADDITIONAL_CONTEXT}}`, … (18 distinct). |

---

## Classes with ZERO findings (named explicitly, v0.16.0 precedent)

- **(b) contradiction (doc↔doc / doc↔repo):** none. Versions (Next.js 15.1.6 / React 18.3.1 / TS5 / Tailwind 3.4.17), the `@/`→root alias, "no `src/` / `app/api/` / `middleware.ts`", "no test runner", region `iad1`, and the `RESEND_API_KEY` dormant-stub framing are stated identically across CLAUDE.md, AGENTS.md, both base rules, both quality-gates, README, DECISIONS, and PHASE9-REPORT, and every claim was verified against the source tree (`PracticeSession` in `session-list.tsx`; `SEED_SESSIONS` in `page.tsx`; TempoBadge thresholds `bpm >= 160`/`>= 100`; `if (!apiKey)` at `lib/email.ts:9`; `SessionList` empty branch at `session-list.tsx:11`; `beat`/`stage`/`accent` tokens in `tailwind.config.ts`; `next-env.d.ts` gitignored).
- **(c) dead-asset:** none. Every rule glob resolves (`app/**/*.tsx`, `components/**/*.tsx` match real files; `app/**/*.ts` is forward-looking, not dead); all gates are runnable (`npx tsc --noEmit`, `npm run lint`, `npm run build` map to real scripts/binaries); no unpassable gate; no placeholder-riddled artifact that can never fire.
- **(e) scope-misroute:** none. Preset = `nextjs` (correct; no headless CMS → base variant correct), tier = Starter (matches operator "just the basics" + no-stakes profile), IDE scope = both, and the negative-signal omissions (no `testing.mdc`/`database.mdc`/`api.mdc`/migration/refactor/seo/i18n/security-auditor, no KB-04/vigilance/surface-routing) all match fixture reality. (The A1 route-mismatch is filed as phantom-reference, not scope-misroute, per one-primary-class.)
- **(f) governance-violation:** none. No pre-existing file was overwritten or deleted; `.env.example` kept byte-identical (diff = IDENTICAL) and correctly excluded from the manifest; `.gitignore` merged append-only (3 lines added, all 21 prior lines preserved) and recorded `disposition: merged-existing`; no `.env` ever read; settings.json denies `Read(.env)`/`Read(.env.*)`. Source-copy files (`app/`, `components/`, `lib/`, configs) are unmodified.
- **(h) duplication-drift:** none. The `npm run lint` / `npx tsc --noEmit` overlap between CLAUDE.md and AGENTS.md is by-design (CLAUDE.md = quirks + Definition of Done; the canonical command *table* lives only in AGENTS.md) and the two copies agree — no divergence. No fact appears in two places with different values.

---

## Methodology note

Fresh-context adversarial sweep against the pristine source, no answer-key access. I read all 44 generated artifacts plus the run report and manifest, then cross-checked in three passes: (1) every absolute `<KIT_ROOT>/...` path and every internal slash-command / skill / agent reference for existence (all kit doctrine paths resolve; all 8 commands resolve; `/simplify` is a legitimate built-in); (2) every factual claim (versions, aliases, thresholds, guards, file locations, tokens, gitignore) grepped against the source tree — all true; (3) placeholder/glob/JSON hygiene, the merge-safety of `.gitignore`/`.env.example`, and manifest integrity. Each candidate was re-opened and quoted before filing; two candidates were dropped after verification (PHASE9-REPORT correctly excluded from the manifest as a run artifact, not a defect; `app/**/*.ts` glob matching no current file is forward-looking, not dead). The one material finding (A1) is the pack shipping full-catalog skill bodies whose routing tables were never pruned to the installed Starter subset — a genuine phantom-route that the kit's own router rule contradicts. Reviewer-variance caveat: A1 sits at the (a)/(e) edge and G1 at the (g)/documented-slot edge; both are filed with the mitigating context stated so a re-judge can re-weigh severity.
