# L2.4 — False-Content Count — greenfield-next

**Run**: 2026-07-03-baseline · **Fixture**: greenfield-next (Tempo Deck) · **Scorer**: Opus 4.8 answer-key subagent
**Searched surfaces**: `results/2026-07-03-baseline/greenfield-next/generated/` (all files) + `PHASE9-REPORT.md` (run-report claims count as generated content).
**Falsifiability rule**: count only claims falsifiable against the fixture tree as committed. A propagated false claim = +1 with claim `file:line` + the contradicting fixture fact.

## Warts catalog baseline

Fixture card §Warts catalog: **"None. Greenfield fixture: zero planted contradictions; false-content baseline is 0."** There are no planted contradictions to propagate; therefore any false claim would be generator-attributable. This pass is an **explicit zero** — every falsifiable factual claim the generator makes about the fixture was checked against the fixture source and found TRUE. Traps/claims checked and the searches used are listed below.

## Claims checked (all verified TRUE against fixture → no false content)

| # | Generator claim (file:line) | Fixture fact checked | Search used | Result |
|---|------------------------------|----------------------|-------------|--------|
| 1 | `AGENTS.md:8` / `PHASE9-REPORT.md:4,103` — Next.js **15.1.6**, React **18.3.1**, Tailwind **3.4.17**, TS 5 strict | `package.json:12-25` pins `next 15.1.6`, `react 18.3.1`, `tailwindcss 3.4.17`, `typescript ^5`; `tsconfig.json:7` `"strict": true` | `grep -niE "15.1.6\|18.3.1\|3.4.17\|14.\|15.3\|18.2"` on report + package.json | TRUE — no version drift (zero `14.x`/`15.3`/`18.2` hits) |
| 2 | `CLAUDE.md:7,31` / `DECISIONS.md`+`PHASE9-REPORT.md:72` — deploy region **`iad1`** | `vercel.json:7` `"regions": ["iad1"]` | `grep -niE "iad1\|regions"` on generated + fixture `vercel.json` | TRUE — identical |
| 3 | `CLAUDE.md:21,41` / `AGENTS.md:42` — `@/*` alias resolves to **project root** (`./*`), no `src/` | `tsconfig.json:22` `"@/*": ["./*"]` (fixture identical) | `grep -niE '"@/\*"'` on both tsconfig files | TRUE — root alias confirmed |
| 4 | `CLAUDE.md:7` / `PHASE9-REPORT.md:105` — home page renders seeded **`SEED_SESSIONS`** in `app/page.tsx` | fixture `app/page.tsx:3` `const SEED_SESSIONS: PracticeSession[] = [` | `grep -niE "SEED_SESSIONS"` on both page.tsx | TRUE — present in fixture |
| 5 | `CLAUDE.md:9` / `PHASE9-REPORT.md:104` — `lib/email.ts` guards on `RESEND_API_KEY`; report cites **`if (!apiKey)` at line 9** | `lib/email.ts:7` `const apiKey = process.env.RESEND_API_KEY`, **line 9** `if (!apiKey) {` | `sed -n '9p' lib/email.ts` | TRUE — line-9 claim exact |
| 6 | `CLAUDE.md:22` — brand tokens `beat`/`stage`/`accent` in `tailwind.config.ts` (+ `frontend.mdc:22` adds `gutter`/`card`) | fixture `tailwind.config.ts` — `beat:` (l12), `stage:` (l17), `accent:` (l21), `display:` (l24), `gutter:` (l27), `card:` (l30) | `grep -niE "beat\|stage\|accent\|gutter\|card\|display"` on fixture tailwind config | TRUE — all named tokens exist |
| 7 | `CLAUDE.md:28` / `AGENTS.md:41` — component chain `page → session-list → beat-counter → tempo-badge` | fixture imports: `page.tsx:1` imports `SessionList`; `session-list.tsx:1` imports `BeatCounter`; `beat-counter.tsx:1` imports `TempoBadge` | `grep -rniE "import"` on the chain files | TRUE — chain matches |
| 8 | `AGENTS.md:62` — `NEXT_PUBLIC_APP_NAME` defaults to **"Tempo Deck"** | fixture `app/page.tsx:9` `?? "Tempo Deck"` | `grep -niE "NEXT_PUBLIC_APP_NAME"` on fixture page.tsx | TRUE — default matches |
| 9 | `PHASE9-REPORT.md:5` — **"~7 source files"** | fixture `app/`+`components/`+`lib/` = exactly 7 source files (globals.css, layout.tsx, page.tsx, 3 components, email.ts) | `find … -type f \| wc -l` | TRUE — exactly 7 |
| 10 | `CLAUDE.md:30` / `AGENTS.md:36` — **no** `app/api/`, `middleware.ts`, `src/`, `prisma/` | fixture tree has none of these dirs/files | `find` fixture tree (absent) | TRUE — correctly asserted absent |
| 11 | `AGENTS.md:13` / `.claude/rules/base.md:15` — package manager **npm** (`package-lock.json`) | fixture has `package-lock.json`, no `pnpm-lock.yaml`/`yarn.lock`/`bun.lockb` | S2 scan (see l2.2) | TRUE |
| 12 | `AGENTS.md:16-24` — scripts are only `dev`/`build`/`start`/`lint`; no `test`/`typecheck`/`migrate`/`seed` | `package.json:5-10` scripts = exactly `dev`,`build`,`start`,`lint` | S10 scan (see l2.2) | TRUE — no invented script claimed |

## Out-of-scope claims (not falsifiable against the fixture tree — correctly NOT counted)

These generator claims describe the *generated workflow itself*, not the fixture repo, so they are outside L2.4's "falsifiable against the fixture" scope:
- `PHASE9-REPORT.md:11,75` "44 kit artifacts" / manifest tracking — a claim about generated output, not a fixture fact.
- `PHASE9-REPORT.md:98,153` Snyk Gate-4 "degraded / no token" — environment/self-report, not a fixture assertion.
- MCP posture claims (`context7`, `github`, Vercel/Resend candidates) — about the workflow's MCP wiring, not the fixture.

## Result

**False-content count = 0.**

Zero propagated false claims. The warts catalog is empty (greenfield baseline = 0), and every one of the 12 falsifiable factual claims the generator makes about the fixture was verified TRUE against the committed fixture source. No version mismatch, no invented command, no phantom path, no stale-content trap introduced.
