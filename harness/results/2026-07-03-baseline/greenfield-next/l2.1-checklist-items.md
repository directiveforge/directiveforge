# L2.1 Checklist Scorer — greenfield-next (Tempo Deck)

**Fixture**: greenfield-next · **Tier**: Starter · **IDE scope**: claude-code + cursor · **Preset**: nextjs
**Checklist**: `generator/VALIDATION_CHECKLIST.md` (17 sections) · **Mode**: static execution against `generated/` tree
**Scorer**: Opus 4.8 L2.1 subagent (no answer key; no FIXTURE-CARD read)
**Date**: 2026-07-03

> Rule: a section PASSES only if ALL applicable items pass. Every N-A carries a tier / IDE / greenfield / environment justification. N/A-until-tier is honest status, not failure (HARNESS-SPEC §3).

---

## Section status table

| § | Section | Status | One-line reason |
|---|---------|--------|-----------------|
| 1 | Token Budget Compliance | **PASS** | CLAUDE.md 97 lines (≤150); 6/6 sections; base.mdc body 30 lines (≤30); alwaysApply body 382 w / 497 tok (≤500 intent met — see item note) |
| 2 | No Redundancy | **PASS** | No fact/table duplicated; the 2 shared commands are quirk-notes with the canonical table deferred to AGENTS.md |
| 3 | All Referenced Paths Exist | **PASS** | All cited dirs exist; `src/`/`app/api/` appear only as negations; frontend globs match real files |
| 4 | All Commands Are Valid | **PASS** | dev/build/start/lint are real scripts; `npx tsc --noEmit`/`npx vercel` valid binaries; npm-only, no mixing |
| 5 | No Secrets or Credentials | **PASS** | Secret-pattern scan clean; MCP configs zero-credential; `.env` never read (only `.env.example`) |
| 6 | Conventions Match Codebase | **PASS** | Named exports + route default, kebab/PascalCase, `@/`→root, no test deps, no ORM — all match source |
| 7 | MCP Security Annotations | **PASS** (1 item N/A) | Both servers annotated (posture+creds); 3 JSONs parse; context7 pinned; github upstream. Snyk item N/A (no token/network) |
| 8 | Skills Include Gotchas | **PASS** | debug + deployment each have Gotchas (3≥2) + When-NOT-to-Use; model-facing triggers; numbered steps, no branches |
| 9 | Agents Include Constraints | **PASS** | 4 agents: `## Constraints` w/ NEVER-ALWAYS, scoped `tools:`, 1-sentence role, defined output, least-privilege |
| 10 | File Count and Completeness | **PASS** | All required files present; domain .mdc only for existing domains; DECISIONS at root; 8 commands (≥3) |
| 11 | Execution Infrastructure | **PASS** | 8 commands; permissions allow+deny; hooks:{} (no formatter, noted); mandatory trio; Plan mode; feedback chain |
| 12 | Decision Skills (KB-05) | **PASS** | 5 BLOCKER skills in both mirrors w/ 5-field frontmatter; HIGH/MED/council correctly absent; router rule present |
| 13 | KB-04 Decision Engineering | **N-A** | Intermediate+ only; Starter tier — all KB-04 artifacts correctly absent |
| 14 | Surface Routing Integration | **N-A** | Intermediate+ only; Starter tier — Phase 4.5 skipped, ai-workflow rule correctly absent |
| 15 | Vigilance Discipline | **N-A** | Advanced-tier opt-in; Starter — operator declined all opt-ins; correctly absent |
| 16 | Mission-Dispatch Pattern | **N-A** | Advanced-tier opt-in; Starter — correctly absent |
| 17 | Cross-Artifact Consistency | **PASS** | Versions/pkg-mgr agree; 12 generated→generated refs resolve; manifest 44 paths + 3 hashes match; DECISIONS↔rules coherent |

**Tally: 13/13 applicable sections PASS. 4 sections N-A (§13–16, tier-gated). 0 FAIL.**

---

## Per-item detail

### §1 Token Budget Compliance — PASS
- ✅ CLAUDE.md ≤150 lines: `wc -l CLAUDE.md` → **97**.
- ✅ Six core sections present/non-empty: Build & Test Commands, Architecture, Key Conventions, Common Pitfalls, Session Protocol, Definition of Done — all matched by `grep "^## .*$s"`.
- ✅ base.mdc body ≤30 lines: `awk '/^---$/{found++;next} found>=2'` → **30** (exactly at ceiling; pass).
- ✅ (note) Total alwaysApply ≤500 tokens: two files are `alwaysApply: true` (`base.mdc`, `quality-gates.mdc`). Body-only (frontmatter stripped) = **382 words → 497 tokens** (≤500, ≤~400 words). Whole-file incl. YAML frontmatter = 410 words / 533 tokens; the 34-word overage is entirely metadata (description/globs), not loaded prose. Scored PASS on the loaded-content intent; flagged as the run's single borderline item.
- decision-skills.mdc + frontend.mdc are `alwaysApply: false` (not counted).

### §2 No Redundancy — PASS
- ✅ No fact in both AGENTS.md and CLAUDE.md: `## Stack` heading exists only in AGENTS.md (0 in CLAUDE.md).
- ✅ Commands not duplicated except by IDE: only `npm run lint` + `npx tsc --noEmit` overlap; CLAUDE.md carries them as the typecheck quirk-note + Definition of Done, and explicitly states "Canonical command table lives in AGENTS.md" — checklist permits this.
- ✅ No base↔frontend rule dup: `comm -12` on `- ` lines returned empty.
- ✅ No skill-content-that-belongs-in-a-rule: skills are procedures (debug/deployment); conventions live in rules.

### §3 All Referenced Paths Exist — PASS
- ✅ Rule dir paths: `.cursor/rules/*.mdc` extraction → all exist. `.claude/rules/*.md` flagged `app/api/` + `src/` but sole hit is base.md:36 "NEVER reference paths that don't exist (no `src/`, no `app/api/`…)" — negations, not live refs.
- ✅ Cited files exist: tailwind.config.ts, tsconfig.json, vercel.json, app/page.tsx, lib/email.ts all present.
- ✅ Frontend globs match: `["app/**/*.tsx","app/**/*.ts","components/**/*.tsx","!**/*.test.*"]` → app/{layout,page}.tsx + components/*.tsx all match.
- ✅ Dead-rule lint repeatability: the glob/path checks are the repeatable mechanism; kit ties them to quarterly health audit (documented).

### §4 All Commands Are Valid — PASS
- ✅ package.json scripts: dev/build/start/lint (`next dev|build|start|lint`).
- ✅ CLAUDE.md cmds: `npm run lint`, `npx tsc --noEmit` — valid. AGENTS.md: dev/build/start/lint + tsc — all valid. base.mdc: dev/build/lint + tsc — all valid.
- ✅ Migration commands: N/A within-item (no ORM) — no migrate command claimed anywhere.
- ✅ Package-manager prefix consistent: only `npm run`; the single yarn/pnpm/bun hit is base.md:15 negation.

### §5 No Secrets or Credentials — PASS
- ✅ Secret-pattern scan (`sk-`/`AKIA`/`ghp_`/`Bearer`): exit 1, clean. `password=`/`secret=` literal scan: empty.
- ✅ MCP config env syntax: `.mcp.json` + `.cursor/mcp.json` use zero-credential servers (github OAuth remote-HTTP, context7 npx no-cred) — no literal creds, no env block needed.
- ✅ `.env` never read: only `.env.example` on disk; `.env`/`.env.local` absent.

### §6 Conventions Match Codebase — PASS
- ✅ Export style: 6 named (`export function`/`export const`/`export type`) + 2 route defaults (page.tsx, layout.tsx `export default`) — exactly matches rule "named for components; route entries also default."
- ✅ File naming: components/ = beat-counter/session-list/tempo-badge.tsx (kebab-case), PascalCase components — matches.
- ✅ Import alias: tsconfig `paths` `{"@/*":["./*"]}` = root — matches rule.
- ✅ Test framework: no vitest/jest/playwright/RTL in package.json — matches "none configured."
- ✅ ORM: no prisma/drizzle/etc — matches "no database."

### §7 MCP Security Annotations — PASS (Snyk item N/A)
- ✅ Every server has posture entry: `.mcp.annotations.md` — github "Posture: RW … via OAuth", context7 "Posture: RO … no credentials."
- ✅ Credentials/permissions stated: github "no PAT env var"; context7 "no credentials."
- ✅ JSON parse: `.mcp.json`, `.cursor/mcp.json`, `.claude/settings.json` all VALID.
- ✅ `${ENV_VAR}` placeholders / no hardcoded creds: security protocol documents `${ENV_VAR}`-for-every-credential; no creds present to placeholder.
- ✅ No experimental server unnoted: only kit-baseline github + context7; Vercel/Resend documented as found-but-not-adopted.
- ✅ Correct filenames: `.cursor/mcp.json` + `.mcp.json`.
- ✅ Pinned versions: context7 `@3.2.2` in both; github remote-HTTP version-managed upstream; no `@latest`.
- 🚫 **N/A**: `uvx snyk-agent-scan@latest` — requires network + `SNYK_TOKEN` (neither available in static-scoring env); degrades to token-required message without a verdict. Environment limitation, not a defect. Both servers independently low-risk by inspection (kit-vetted).

### §8 Skills Include Gotchas — PASS
- ✅ `## Gotchas` ≥2: debug 3 items, deployment 3 items.
- ✅ `## When NOT to Use`: present in both.
- ✅ Trigger model-facing: both open `> **Trigger**: Use this skill when…`.
- ✅ Numbered/sequential, no ambiguous branches: `grep "you can also"/"alternatively"` empty.

### §9 Agents Include Constraints — PASS
- ✅ `## Constraints` w/ NEVER/ALWAYS: reviewer(3), simplifier(3), tester(4), verifier(5).
- ✅ Tool access explicit: reviewer/simplifier `Read, Grep`; verifier `Bash, Read`; tester `Read, Grep, Write, Bash`.
- ✅ Role one sentence: each `**Role**:` is single-sentence.
- ✅ Output defined: reviewer/simplifier/verifier have explicit Output/Report sections; tester defines return via protocol step 4 ("run tests; report coverage gaps") + "ALWAYS run tests before reporting completion" (implicit-but-present contract).
- ✅ Least privilege: read-only agents hold no write; Bash agents scope command families in-body (verifier: "restricted to `npx tsc --noEmit`, `npm run lint`, `npm run build` ONLY"; tester: "test command … plus one-time install if approved — never silent network installs").

### §10 File Count and Completeness — PASS
- ✅ AGENTS.md, CLAUDE.md at root. base.mdc in `.cursor/rules/` (Cursor in scope).
- ✅ Domain .mdc only for existing domains: frontend.mdc present; api/database/testing.mdc correctly absent (no such layers) — per PHASE9.
- ✅ Skills only for applicable procedures: debug + deployment (Cursor); 5 decision skills.
- ✅ `.cursor/mcp.json` + `.mcp.json` present (correct formats).
- ✅ DECISIONS.md at root; no competing `governance/`/`docs/` ledger (both absent) — no duplication.
- ✅ `.cursorignore` + `.cursorindexingignore` present (Cursor in scope).
- ✅ ≥3 `.claude/commands/`: **8** (review-pr + update-context + deploy [deployable] + 5 more).

### §11 Execution Infrastructure — PASS
- ✅ ≥5 slash commands: 8 (review-pr, deploy, update-context, discover-mcp, security-review, tech-debt, workflow-help, status). Daily-workflow coverage satisfied; illustrative names in checklist are not literal requirements.
- ✅ PostToolUse hooks: `hooks: {}` with report note "no formatter detected" — the checklist's explicit fallback for no-formatter projects.
- ✅ Permissions: `allow` 15 (git reads, add/commit, build/lint/dev, `npx tsc`, ls/wc/grep/which); `deny` 5 (`rm -rf`, `git push --force`, `git reset --hard`, `Read(.env)`, `Read(.env.*)`).
- ✅ MCP servers: `.mcp.json` has github + context7 (project-relevant: Next.js docs + GitHub).
- ✅ Feedback loop chain: CLAUDE.md Definition of Done (`tsc --noEmit` + `lint`) + `.claude/rules/quality-gates.md` gate table (typecheck/lint/build).
- ✅ Mandatory trio: simplifier + reviewer + verifier all in `.claude/agents/` (+ tester).
- ✅ Plan mode: CLAUDE.md:56 "Before complex tasks: use Plan mode."

### §12 Decision Skills (KB-05) — PASS
- ✅ 5 BLOCKER skills in `.claude/skills/decision/` AND `.cursor/skills/decision/` (Cursor in scope): pre-mortem, steelman, confidence-calibration, reversibility-check, anti-sycophancy-meta.
- ✅ 4 HIGH skills: correctly ABSENT (Intermediate+ only; this is Starter).
- ✅ 3 MEDIUM skills: correctly ABSENT (Advanced only).
- ✅ Council: correctly ABSENT (Advanced + plan-tier ≥ Max only).
- ✅ Each SKILL.md frontmatter: name (matches slug), description (pushy/model-facing), severity (BLOCKER), confidence (0.85–0.95), surface (Chat-or-Code / All) — 5/5 populated across all 5.
- ✅ Router rule `.claude/rules/decision-skills.md`: present; phrase-mapping table (7 pipe-rows) + graduation note; council 15× gate item is conditional ("if council installed") — correctly not present since council not installed.

### §13 KB-04 Decision Engineering — N-A
🚫 Intermediate+ only. Starter tier (manifest `maturity_tier: Starter`; PHASE9 confirms). All artifacts (ARCHITECT_PROMPT template, orchestrator-dispatch/research rules, research-prompt-writer/synthesizer skills) verified ABSENT — correct tier-gated state, not a failure. DECISIONS.md uses Tier 1 Starter format (single-line entries), which is the tier-appropriate format.

### §14 Surface Routing Integration — N-A
🚫 Intermediate+ only. Phase 4.5 skipped at Starter (PHASE9). `.claude/rules/ai-workflow.md` + `.cursor/rules/ai-workflow.mdc` verified ABSENT — correct tier-gated state.

### §15 Vigilance Discipline — N-A
🚫 Advanced-tier opt-in. Operator declined all opt-ins (PHASE9); Starter tier. All vigilance artifacts (KIT-VIGILANCE, WATCHLIST, vigilance/state, dispatch prompts) verified ABSENT. Per checklist: "offered and declined → N/A." Correct.

### §16 Mission-Dispatch Pattern — N-A
🚫 Advanced-tier opt-in. Starter tier; declined. chat-research-missions / code-handoff-prompts / cowork-browser-operations / prompts-dispatch / research-dir all verified ABSENT. Per checklist: "offered and declined → N/A." Correct.

### §17 Cross-Artifact Consistency — PASS
- ✅ Fact agreement: Next.js "15.1"×2 + "15.1.6"×1 (truncation, consistent w/ package.json `15.1.6`); React "18.3"/"18.3.1" (consistent w/ `18.3.1`). Pkg-mgr: `npm run`×21; single yarn/pnpm/bun hit is base.md:15 negation.
- ✅ Generated→generated refs resolve: 12 backtick-quoted cross-doc paths (`.claude/agents/verifier.md`, `.claude/rules/decision-skills.md`, `.mcp.annotations.md`, AGENTS.md, tailwind.config.ts, etc.) all exist on disk.
- ✅ Manifest integrity: `python3 -m json.tool` valid; files[] = 44; all 44 listed paths exist; 3 sha256 spot-checks (CLAUDE.md, settings.json, pre-mortem SKILL.md) match `shasum -a 256`; `maturity_tier: Starter` / `ide_scope: [claude-code,cursor]` / `presets: [nextjs]` all match PHASE9-REPORT.
- ✅ Doc-derived checks executed: PHASE9-REPORT "Unit tests for English" block records 5 falsifiable checks (all PASS).
- ✅ DECISIONS↔rules coherence: DECISIONS reserves no-DB / `@/`→root / named+default exports / dormant email stub / Vercel-hobby — rules mandate exactly these; zero contradiction.

---

## N/A item register (with justification)

| Item | Section | Reason |
|------|---------|--------|
| `uvx snyk-agent-scan@latest` full verdict | §7 | Requires live network + `SNYK_TOKEN` (unavailable in static-scoring env); degrades to token-required message. Environment limitation. |
| KB-04 rules/skills/architect-prompt | §13 (whole) | Intermediate+ only; project is Starter tier. Correct tier-gated absence. |
| ai-workflow surface-routing rule | §14 (whole) | Intermediate+ only; Phase 4.5 skipped at Starter. |
| Vigilance bootstrap (routines/watchlist/dispatch) | §15 (whole) | Advanced opt-in; operator declined; Starter tier. |
| Mission-dispatch prompt libraries | §16 (whole) | Advanced opt-in; operator declined; Starter tier. |
| Live build/lint/typecheck execution | §4/§11 (wiring verified statically) | Fixture has no `node_modules`; scripts + tsconfig verified static. Not a checklist item per se; noted for completeness. |

---

## Sign-off

```
Project: Tempo Deck (greenfield-next fixture)
Generator run: 2026-07-03 · Kit 0.17.0 · Starter · nextjs · claude-code+cursor
Applicable sections: 13 PASS / 13
N-A sections: 4 (§13 §14 §15 §16 — tier-gated)
Sections FAILED: none
Borderline (scored PASS): §1.4 alwaysApply token budget — 497 tok body-only (≤500) vs 533 whole-file
Validator: Opus 4.8 L2.1 checklist scorer (no answer key)
```
