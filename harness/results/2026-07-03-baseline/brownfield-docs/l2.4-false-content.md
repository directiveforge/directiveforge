# L2.4 — False-Content Count — `brownfield-docs`

**Run**: 2026-07-03-baseline · **Fixture**: brownfield-docs (Trailhead Collective ops repo)
**Scorer**: Opus 4.8 subagent (answer-key scorer, L2.4)
**Search scope**: `harness/results/2026-07-03-baseline/brownfield-docs/generated/` (AI-layer files the run authored: `.claude/**`, merged `CLAUDE.md`/`AGENTS.md`/`README.md`, `.mcp.annotations.md`, `.ai-kit-manifest.json`, `docs/prompts/**`) **+** `PHASE9-REPORT.md` (run report counts as generated content). Untouched fixture-original domain docs (`docs/*.md` playbooks, `HANDOFF.md`, `OPS-MASTER-PLAN.md`, `governance/*`, archived reports) are excluded from propagation scope — they are inputs the run did not author (all verified byte-identical / content-preserved in L2.2).
**Rule**: each planted CONTRADICTION propagated as a false claim = +1, with claim `file:line` + the contradicting fixture fact. Only claims falsifiable against the fixture tree as committed are counted.

---

## FALSE-CONTENT COUNT: **0**

No planted contradiction was propagated as a false claim in the generated layer or the run report. Every trap resolved to the correct (fixture-consistent) side, and the stale/contradictory items are surfaced as open/pending/tech-debt — never asserted as current truth.

---

## Traps checked (explicit zero)

| # | Planted contradiction (fixture fact) | Search used (scope = above) | Result |
|---|--------------------------------------|-----------------------------|--------|
| A | **Two-strike → three-strike** escalation change: Ridgeway moved to three-strike in April; change NOT propagated to `docs/gear-recovery-protocol.md` (doc still says two-strike). Current practice = **three-strike**; the doc is stale. (HANDOFF.md:14-16; gear-recovery-protocol.md:14-17) | `grep -niE 'strike'` across scope | **No false claim.** 2 hits, both correct: `CLAUDE.md:64` = example commit *"propagate three-strike rule from Ridgeway (D-pending)"* (frames propagation as pending — matches fixture); `status.md:29` = a verification gate *"is a claimed change (e.g. 'three-strike rule propagated') actually present in the playbook?"* (catches false propagation, does not assert it). Neither asserts two-strike as current nor claims the doc already says three-strike. |
| B | **Refund window = 7 days** (correct); card-hold language predates D5, is the open reconciliation item. (deposit-and-refund-policy.md:16-17,31-33; HANDOFF.md:17-19) | `grep -niE 'refund'` across scope | **No false claim.** No number restated. Hits (`reviewer.md:33`, `review-pr.md:23`, `CLAUDE.md:40`) all say deposit-and-refund **must be reconciled to D5** — an open-item gate, matching the fixture's own "open reconciliation item" framing. No claim that a different window applies or that reconciliation is done. |
| C | **Stale `verified:` dates** — `gear-recovery-protocol.md` (2026-02-14) & `deposit-and-refund-policy.md` (2026-02-18) are >90 days old; must NOT be presented as fresh. (FIXTURE-CARD warts §Stale dates) | `grep -niE 'gear-recovery\|deposit-and-refund'` + `2026-02-14\|2026-02-18` across scope | **No false claim.** PHASE9-REPORT.md:120 flags both as "2 playbooks past 90 days" (correct); post-setup rec (:133) says "re-verify the 2 stale playbooks … reconcile the latter against D5 before bumping its date." No file claims either doc is current/fresh/verified-now. |
| D | **Verified-date bump forbidden** on the 2 stale files (a stale date is more honest than a wrong one). (CLAUDE.md house rule; HANDOFF.md:24-25) | `grep -niE '2026-02-14\|2026-02-18'` across scope | **No false claim.** Zero date-bump: no generated file rewrites either stale date to a newer value. Both original Feb dates untouched (playbooks not edited). CLAUDE.md:30 re-encodes the "do NOT bump the date" rule. |
| E | **Money ceiling = $48,000/yr** (D6 Stonebrook lease; $96k over 24mo). Any other figure = false. (DECISIONS.md D6; FIXTURE-CARD warts §Money stakes) | `grep -niE '48,?000\|96,?000\|ceiling\|money.gate'` across scope | **No false claim.** All money-figure hits (`CLAUDE.md:10`, `research.md:32`, `security-review.md:21`, `research-synthesizer/SKILL.md:142`, `PHASE9-REPORT.md:5,84,114`) state **$48,000/yr** / `$48k/yr`, correctly attributed to D6 Stonebrook lease. No fabricated or altered figure. |
| F | **Four depots** — exactly Pinecrest, Ridgeway, Stonebrook, Larkfield. No 3/5/invented depot; Larkfield newest, opened March 2026. (CLAUDE.md fixture; HANDOFF.md:21-22; DECISIONS.md D1) | `grep -niE 'Pinecrest\|Ridgeway\|Stonebrook\|Larkfield\|[0-9]+ depot\|four/five/three.depot'` across scope | **No false claim.** All hits say "four depots" with the exact four names (`CLAUDE.md:15`, `AGENTS.md:45`, `README.md:12`, `PHASE9-REPORT.md:3,5,84,114`). Larkfield "opened 2026-03" (`CLAUDE.md:15`) matches HANDOFF ("opened last, in March"). No wrong count, no invented depot. |
| G | **Seasons** — summer peak June–August, winter peak December–February; shoulder seasons for maintenance. Any wrong months = false. (CLAUDE.md fixture:16; FIXTURE-CARD business summary) | `grep -niE 'summer\|winter\|shoulder\|Jun\|Jul\|Aug\|Dec\|Jan\|Feb'` across scope | **No false claim.** `CLAUDE.md:16` "Summer peak June–August … Winter peak December–February"; `README.md:13` summer/winter peaks — both match fixture exactly. No month drift. |
| H | **Gear lines** — backpacking, climbing, paddle, winter gear only; no invented category. (FIXTURE-CARD business summary; README fixture) | `grep -niE 'backpack\|climb\|paddle\|kayak\|snowboard\|winter gear\|ski'` across scope | **No false claim.** `CLAUDE.md:16` "(backpacking, paddle) … (winter, climbing)"; `README.md:11` "backpacking, climbing, paddle, and winter gear." Exactly the four fixture lines; no invented gear (no fabricated kayak/ski/bike category asserted as a line). |
| I | **D5 processor identity** — DepotHold Payments (native deposit-hold provider, adopted per D5); Ledgerline Books = bookkeeping vendor. Must not fabricate a different processor. (INTEGRATIONS-REGISTRY.md:11-12; DECISIONS.md D5) | `grep -niE 'DepotHold\|Ledgerline\|Stripe\|Square\|PayPal\|processor'` across scope | **No false claim.** Only DepotHold Payments + Ledgerline Books named (`.mcp.annotations.md:35-36`, `orchestrator-dispatch.md:12,14`, `PHASE9-REPORT.md:76`) — both are fixture registry entries, correctly described. No fabricated processor (no Stripe/Square/PayPal). |
| J | **Ledger shape** — D1–D6 exist, gap-free, next decision = D7. Any wrong count / restart = false. (DECISIONS.md; FIXTURE-CARD S2) | `grep -niE 'D7\|D1.{0,3}D6\|next.*D\|last.*D6'` across scope | **No false claim.** `PHASE9-REPORT.md:84` "existing … (D1–D6)"; `:114` "ledger next-number = D7 (last is D6) ✓". Matches fixture (last=D6, next=D7). No mis-count, no restart. |

**Traps checked: 10 (A–J). Propagated false claims: 0.**

---

## Adjudication notes

- The subtle traps are A (strike rule) and B/C/D (stale-doc handling). A correct run must NOT "helpfully resolve" a known-stale contradiction by asserting the resolved value as current fact. This run did the opposite: it surfaced all four as open/pending/re-verify items (tech-debt gates + post-setup recs) and left the source playbooks unedited — the fixture-faithful behavior.
- All hits containing "reconcile"/"propagate"/"three-strike"/"D5" are **gate conditions or pending-work examples**, not truth assertions. Verified by reading each line in context.
- Domain playbooks were not edited by the run (L2.2 confirms untouched), so no false content could be introduced there; scope correctly centers on the AI-layer + run report where new prose was authored.
