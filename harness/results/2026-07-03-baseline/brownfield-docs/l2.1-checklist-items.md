# L2.1 Checklist Scorer — fixture `brownfield-docs`

**Target tree**: `<KIT_ROOT>/harness/results/2026-07-03-baseline/brownfield-docs/generated/`
**Checklist**: `generator/VALIDATION_CHECKLIST.md` (17 sections)
**Applicability context**: tier=**Advanced**, ide_scope=**[claude-code]** (Cursor OUT), presets=**[docs-ops]** (no-manifest / no-runtime corpus), packs=**[kb-04, kb-05-decision]**. Ledger pre-exists at `governance/DECISIONS.md` (house `D{N}` format). MCP governance branch fired (D4) → no `.mcp.json` wired by design.
**Scored by**: Opus 4.8 L2.1 subagent (static execution; no answer key consulted).

Section passes only if ALL applicable items pass. N-A items carry a tier/IDE/greenfield/environment justification (N/A-until-scaffold is honest status, not failure — HARNESS-SPEC §3).

---

## Section status table

| # | Section | Status | One-line basis |
|---|---------|--------|----------------|
| 1 | Token Budget Compliance | **PASS** | CLAUDE.md 80 lines (≤150); all six sections present-in-spirit ("Build & Test Commands" replaced by Session-Protocol rituals per docs-ops no-runtime adaptation); Cursor base.mdc N/A; always-loaded rules ~2555 words but these are conditionally-loaded `.claude/rules/`, not Tier-1 alwaysApply |
| 2 | No Redundancy | **PASS** | AGENTS.md = interop summary + explicit "authoritative copy in CLAUDE.md"; no commands to duplicate (no build system); no rule/skill content misplacement |
| 3 | All Referenced Paths Exist | **PASS** | Every project-relative + kit-absolute path resolves; 2 apparent "MISSING" are false positives (prohibition prose + truncated absolute path) |
| 4 | All Commands Are Valid | **N-A** | No `package.json`/manifest — no build/test/migration commands exist to validate (docs-ops no-manifest preset) |
| 5 | No Secrets or Credentials | **PASS** | Secret-pattern scan clean; no `.env` present or read; D4 forbids in-repo credentials |
| 6 | Conventions Match Codebase | **PASS** (adapted) | Docs-first conventions (frontmatter `owner`/`verified:`/`sources:`, kebab-case, single-source-of-truth) match the 9 real playbooks; code-export/ORM/test items N/A (no code) |
| 7 | MCP Security Annotations | **PASS** (adapted) | No server wired (D4 governance branch); `.mcp.annotations.md` documents propose-only posture + bootstrap; `.claude/settings.json` valid strict JSON; snyk scan N/A (nothing wired to scan) |
| 8 | Skills Include Gotchas | **PASS** | All 13 SKILL.md carry `## Gotchas` (≥2 items each: 3–6) + When-NOT; trigger descriptions model-directed; steps numbered |
| 9 | Agents Include Explicit Constraints | **PASS** | Trio each has `## Constraints`, `## Tools` (reviewer/simplifier=Read,Grep; verifier=Bash,Read), `## Output Format`; least-privilege honored |
| 10 | File Count and Completeness | **PASS** | AGENTS.md + CLAUDE.md at root; ledger at `governance/` not duplicated; 7 commands (>3, deploy correctly omitted — not deployable); `.mcp.json` intentionally absent (D4); Cursor items N/A |
| 11 | Execution Infrastructure | **PASS** (adapted) | 7 commands (≥5); `hooks: {}` (no formatter — allowed w/ note); allow/deny permissions set; feedback loop = doc-lint gates; trio present; Plan mode in session protocol |
| 12 | Decision Skills Installation (KB-05) | **PASS** | 5 BLOCKER + 3 HIGH + 3 MEDIUM = 11 installed; all frontmatter fields present; council-3-voice correctly omitted (no Max evidence) + flagged in router; router rule present w/ council token-cost gate |
| 13 | KB-04 Decision Engineering (Intermediate+) | **PASS** | Architect template + orchestrator-dispatch + research rules + both research skills installed; ledger carries reversal-via-supersession + cadence discipline |
| 14 | Surface Routing Integration (Intermediate+) | **N-A / pending** | `.claude/rules/ai-workflow.md` absent; Phase 4.5 deferred to a Cowork handoff per report — "pending operator action", not a failure (checklist §14 escape clause) |
| 15 | Vigilance Discipline (Advanced opt-in) | **N-A** | Offered, operator declined (Phase 7) |
| 16 | Mission-Dispatch Pattern (Advanced opt-in) | **N-A** | Offered, operator declined (Phase 8) |
| 17 | Cross-Artifact Consistency | **PASS** | kit_version 0.17.0 uniform; tier/scope/presets/packs match report; manifest valid JSON, 34 files all exist, not self-listed, 3-hash spot-check MATCH; generated↔generated refs resolve; no dangling ref to pending ai-workflow.md |

**Tally: 12/12 applicable sections PASS.** (§4, §14, §15, §16 = N-A with justification; 13 sections carry a definite PASS/N-A verdict, of which 12 are applicable-and-passing and 4 are N-A.)

---

## Per-item detail

### §1 Token Budget — PASS
- `wc -l CLAUDE.md` → **80** (≤150). PASS.
- Six-section grep: only `Build & Test Commands` reported MISSING. Other five present (`Architecture`, `Key Conventions`, `Common Pitfalls`, `Session Protocol`, `Definition of Done`) + `Critical Constraints`, `Domain Facts`. **Adaptation accepted**: docs-ops no-runtime corpus has no build/test commands; Session-Protocol operating rituals substitute (report Gap #4). Item passes under the "short file carrying all six [in-spirit]" allowance.
- `base.mdc`: **N/A** — Cursor OUT of scope (no `.cursor/`).
- alwaysApply rules token budget: `wc -w .claude/rules/*.md` = 2555 words total. These are Claude Code `.claude/rules/` (conditionally surfaced / router-class), NOT Cursor `alwaysApply:true` Tier-1 files; the ≤400-word alwaysApply gate targets the Cursor always-loaded surface, which is out of scope. No Tier-1 always-load violation. PASS.

### §2 No Redundancy — PASS
- AGENTS.md header explicitly states "Full house rules and constraints live in `CLAUDE.md`; this file is the portable interop summary — it does not restate the constraint list"; its "Key constraints" block is labeled "summary — authoritative copy in `CLAUDE.md`". Deliberate summary-vs-authoritative split, not drift-prone duplication.
- No commands duplicated (no build commands exist).
- No `base.md`↔domain-rule content dup observed; skills are procedures, rules are conventions — correct placement.

### §3 Referenced Paths — PASS
- Narrow-regex dir check: `docs/archive/` OK.
- Broadened check over CLAUDE.md/AGENTS.md/rules: all resolve. Two apparent misses disambiguated:
  - `DECISIONS.md` bare → always in prohibition prose ("never create a root `DECISIONS.md`"), not a reference to an existing file. Not a path assertion.
  - `KB-05-CONVERSATIONAL-DECISION-ENGINEERING.md` → tail of absolute path `<KIT_ROOT>/knowledge-base/KB-05-...` (truncated by grep); file EXISTS.
- All 9 kit-absolute paths cited resolve (KB-02/04/05, MCP registry, surface-routing, docs-ops preset, checklist, code-handoff, cowork-browser).
- Dead-rule lint repeatability: quality-gates.md wires the link/index/ledger checks + verifier agent runs them mechanically. Satisfied.

### §4 Commands Valid — N-A
- No `package.json` / `requirements.txt` / manifest. No `npm/pnpm/yarn/bun` command strings in CLAUDE.md/AGENTS.md/rules. No migration commands. Nothing to validate against a script table. Justification: docs-ops no-manifest preset (no runtime).

### §5 No Secrets — PASS
- `grep -rE` secret-shape scan across CLAUDE.md/AGENTS.md/.claude/.mcp.annotations.md/manifest → **no matches**.
- No `.env` / `.env.example` present (zero env vars in corpus; D4 bans in-repo credentials). `.env` never read.
- MCP `${ENV_VAR}` item: N/A — no MCP config exists; annotations doc instructs future founder to use `${ENV_VAR}`.

### §6 Conventions Match — PASS (adapted)
- Rules mandate frontmatter `owner`+`verified: YYYY-MM-DD`+`sources:`, kebab-case filenames, sentence-case headings, single-source-of-truth. Sampled `docs/gear-recovery-protocol.md` → matches (owner: founder / verified: 2026-02-14 / sources: ledger+master-plan). All 9 playbooks kebab-cased.
- Code-specific items (export style, ORM patterns, test framework, import alias): **N/A** — no code/tsconfig/ORM/test packages in a Markdown corpus.

### §7 MCP Security Annotations — PASS (adapted)
- No `.mcp.json` / `.cursor/mcp.json` — governance branch fired on D4 ("no external integration without founder bootstrap"). Every candidate (github, context7, DB/payments/CMS) recorded in `.mcp.annotations.md` as propose-only/not-applicable with posture (RO/RW), credential surface, and registry gate.
- `python3 -m json.tool < .claude/settings.json` → VALID.
- `${ENV_VAR}` / pinned-version / correct-filename items: N/A (no config wired); annotations doc prescribes them for any future founder bootstrap.
- `uvx snyk-agent-scan`: **N/A** — nothing wired to scan (governance left `.mcp.json` absent by design); note in annotations that any future server must be scanned before first use. (Also a network/live-install item, out of static scope.)

### §8 Skills Gotchas — PASS
- All 13 SKILL.md: `## Gotchas` present with ≥2 items (counts 3–6) AND `## When NOT` present (anti-sycophancy-meta shows 2 When-NOT-matching headings; ≥1 satisfies).
- Trigger descriptions model-directed (e.g. pre-mortem: "Fires on phrases like 'I'm about to launch X'... Use this whenever a user brings a plan they own").
- research-synthesizer: 18 numbered steps — sequential, not "you can also" branches.

### §9 Agents Constraints — PASS
- reviewer.md / simplifier.md / verifier.md each have `## Constraints`, `## Tools`, `## Output Format`.
- Tool access explicit & least-privilege: reviewer=Read,Grep · simplifier=Read,Grep · verifier=Bash,Read (read-only Bash for gate-running). No agent has write access it doesn't need.
- Role descriptions are one sentence + a delegation cue.

### §10 File Count / Completeness — PASS
- `AGENTS.md` ✓ root · `CLAUDE.md` ✓ root (merged from existing, backup at `CLAUDE.md.backup`).
- Cursor items (base.mdc, domain .mdc, .cursor/mcp.json, .cursorindexingignore, .cursorignore): **N/A** — Claude-only scope.
- `.mcp.json` (Claude format): intentionally absent — D4 governance branch (correct deviation, report Gap #2). Not a miss.
- `DECISIONS.md`: exists at `governance/DECISIONS.md` (pre-existing house ledger); NO root duplicate created (correct — Phase 4.3 never-fork-a-ledger).
- Commands: 7 (`review-pr`, `update-context` + `tech-debt`, `discover-mcp`, `security-review`, `workflow-help`, `status`). ≥3 satisfied; `deploy` correctly omitted (not deployable).

### §11 Execution Infrastructure — PASS (adapted)
- Slash commands ≥5: 7 present. PASS.
- PostToolUse hooks: `hooks: {}` — explicitly allowed by the checklist ("`{}` with a report note when no formatter exists"); no Markdown formatter toolchain (report + annotations note it). PASS.
- Permissions: `allow` (16 entries: git reads, ls/wc/grep/find/cat/head/tail/awk/sed) + `deny` (5: rm -rf, force push, hard reset, `.env` reads). PASS.
- MCP servers: none — D4 governance (adapted, PASS).
- Feedback-loop chain: doc-lint gates (link health / index drift / ledger integrity / source reconciliation) in CLAUDE.md Definition-of-Done + quality-gates.md substitute for build+lint+test. PASS.
- Trio subagents: Simplifier + Reviewer + Verifier present. PASS.
- Plan mode: CLAUDE.md Session Protocol line 48 "Use Plan mode before multi-file doc changes." PASS.

### §12 Decision Skills (KB-05) — PASS
- 5 BLOCKER installed: pre-mortem, steelman, confidence-calibration, reversibility-check, anti-sycophancy-meta ✓ (Cursor mirror N/A — Claude-only).
- 4 HIGH (Intermediate+): inversion, second-order-thinking, disconfirming-evidence-first ✓ (steelman counted in BLOCKERs).
- 3 MEDIUM (Advanced): 10-10-10, cost-of-inaction, bayesian-update ✓.
- Council skill (Advanced AND plan-tier ≥ Max): **correctly NOT installed** — no Max evidence; router documents the absence + no-council fallback + ~15× token-cost gate.
- Frontmatter: all 11 carry `name` (matches slug) + `description` + `severity` + `confidence` + `surface`.
- Router rule `.claude/rules/decision-skills.md` present: phrase-map + anti-sycophancy auto-fire + council cost gate documented.

### §13 KB-04 (Intermediate+) — PASS
- `docs/prompts/ARCHITECT_PROMPT.template.md` ✓.
- `.claude/rules/orchestrator-dispatch.md` ✓ · `.claude/rules/research.md` ✓ (Cursor mirrors N/A).
- `.claude/skills/research-prompt-writer/SKILL.md` ✓ · `.claude/skills/research-synthesizer/SKILL.md` ✓ (Cursor mirrors N/A).
- DECISIONS format: pre-existing house `D{N}` ledger with append-only + reversal-via-supersession + documented cadence. Not the kit's literal Tier-2/Tier-3 template, but honored as source-of-truth (Phase 4.3 — forking would break the audit trail); the reversal-trigger + re-verification discipline the item wants is present in the ledger header + orchestrator-dispatch/research rules that point at it. PASS.

### §14 Surface Routing (Intermediate+) — N-A / pending
- `.claude/rules/ai-workflow.md`: **absent**. `.cursor/rules/ai-workflow.mdc`: N/A (Claude-only).
- Report Phase 4.5 produced a Cowork handoff block; integration deferred to a fresh Cowork session. Checklist §14 escape clause: "If Phase 4.5 was deferred ... this section may be flagged 'pending operator action' rather than failed." Flagged **pending operator action**, not a failure. CLAUDE.md correctly carries NO dangling reference to the not-yet-created rule.

### §15 Vigilance (Advanced opt-in) — N-A
- Offered per Advanced tier, operator declined (Phase 7). Checklist: "If Phase 7 was offered and declined ... this section is N/A." No `workflows/KIT-VIGILANCE.md` / `vigilance/` tree present — consistent with decline.

### §16 Mission-Dispatch (Advanced opt-in) — N-A
- Offered, operator declined (Phase 8). Checklist: "If Phase 8 was offered and declined, this section is N/A." No `prompts/chat-research-missions.md` / `prompts/dispatch/` / `research/` tree — consistent with decline.

### §17 Cross-Artifact Consistency — PASS
- Fact agreement: `kit_version` 0.17.0 uniform across all 34 file entries + header. No package-manager prefix or stack-version to diverge (no runtime). Tier/ide_scope/presets/packs in manifest header (Advanced / [claude-code] / [docs-ops] / [kb-04, kb-05-decision]) MATCH the Phase 9 report.
- Generated↔generated refs resolve: all `.claude/{rules,agents,skills}` cross-citations resolve; no dangling reference to the pending `ai-workflow.md`.
- Manifest integrity: `python3 -m json.tool` PASS; 34 files, **all paths exist**; manifest not self-listed; 3-hash spot-check (`CLAUDE.md`, `AGENTS.md`, `.claude/settings.json`) → **MATCH**.
- Doc-derived falsifiable checks: ledger next-number D7 (last D6, monotonic D1–D6 gap-free) ✓; four depot names present ✓; 9/9 playbooks carry frontmatter ✓; router lists only installed skills + flags council NOT installed ✓.
- DECISIONS↔rules coherence: no rule mandates a pattern the ledger reserves; council-3-voice absence framed consistently in router + report + manifest notes.

---

## Live doc-lint dry-run (pre-existing fixture drift — NOT a generation defect)

The generated gates, when run against the live corpus, surface **domain-content** drift in files the install did not touch (per "never invent depot content" + evolve-scope). These are findings for the operator, and their presence in untouched domain files does NOT fail any generated-artifact section:

- **Index drift**: `docs/INDEX.md` links 2 dead rows — `depot-closing-checklist.md`, `gear-replacement-cost-list.md` (neither exists). `docs/INDEX.md` is untouched domain content, not a generated file → does not fail §17 (which scores the generated artifact graph).
- **Stale `verified:`**: 2 playbooks past 90 days (`gear-recovery-protocol.md` 2026-02-14, `deposit-and-refund-policy.md` 2026-02-18) — exactly the two HANDOFF.md already flags. Corpus internally consistent about its known-stale set.
- **Ledger**: D1–D6 monotonic, gap-free. Clean.

These confirm the generated gates WORK (they catch real drift) — a positive signal for §11/§17, not a failure.
