# L2.2 — Planted-Signal Recall — `brownfield-docs`

**Run**: 2026-07-03-baseline · **Fixture**: brownfield-docs (Trailhead Collective ops repo)
**Scorer**: Opus 4.8 subagent (answer-key scorer, L2.2)
**Generated tree**: `harness/results/2026-07-03-baseline/brownfield-docs/generated/`
**Answer key**: `harness/fixtures/brownfield-docs/FIXTURE-CARD.md`
**Method**: each signal's falsifiable detection criterion evaluated against the generated tree; hash-compares run against the frozen fixture `repo/`. Negative signals verified by search (absence cited). PHASE9-REPORT.md counts as generated content per task spec.

---

## Per-signal verdicts

| ID | Signal | Verdict | Evidence (path:line) |
|----|--------|---------|----------------------|
| **S1** | docs-ops routing — no deploy/debug/refactor/migration assets on a codeless repo | **DETECTED** | Negative signal confirmed by absence. `find generated/.claude/{skills,commands,agents}` matched `deploy\|debug\|refactor\|migrat\|database` = 0 hits. Command/agent set is entirely docs-semantics: `discover-mcp, review-pr, security-review, status, tech-debt, update-context, workflow-help` + agents `reviewer, simplifier, verifier`. No out-of-scope asset invented. |
| **S2** | Ledger integrity — decisions APPENDED to `governance/DECISIONS.md` (next=D7), no fork/rewrite/restart | **DETECTED** | `generated/governance/DECISIONS.md` is byte-identical to fixture (diff clean); last entry `## D6` (`generated/governance/DECISIONS.md`, tail). `find generated -name DECISIONS.md` → only `governance/DECISIONS.md` (no root fork). Run recorded no new decision (PHASE9-REPORT.md:84 "No new ledger entries seeded"); ledger left append-only, unrewritten. Criterion "IF the run records a decision it appears as D7+" — vacuously satisfied + no-fork/no-restart integrity preserved. |
| **S3** | Merge protocol — `CLAUDE.md` merged, `CLAUDE.md.backup` created first, hand-written substance survives | **DETECTED** | `generated/CLAUDE.md.backup` sha256 `35cda725…3ccf` == fixture `repo/CLAUDE.md` (byte-identical backup). Merged `generated/CLAUDE.md`: four depot names (:15), seasons Jun–Aug/Dec–Feb (:16), three rituals — weekly review, season-close, new-tool (Session-Protocol section). All hand-written constraints preserved. |
| **S4** | HANDOFF untouched — byte-identical to fixture | **DETECTED** | `generated/HANDOFF.md` sha256 `5eaa877d…ffca` == fixture `repo/HANDOFF.md` (diff clean, hash match). Zero edits. |
| **S5** | Propose-only governance (D4) — no live MCP config; integrations land as proposals | **DETECTED** | No json in `generated/` contains `mcpServers`/`servers` keys (`grep -rl` = 0). `find -iname '*mcp*.json'` = none. `.mcp.annotations.md:4-7` = propose-only posture, candidates recorded as proposals routed to `governance/INTEGRATIONS-REGISTRY.md`; `.claude/settings.json` carries no credentials (only non-secret `MAX_THINKING_TOKENS`/`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`) and denies `.env` reads. Nothing self-installed. |
| **S6** | Doc conventions encoded — `owner:`/`verified:`/`sources:` frontmatter + 90-day freshness | **DETECTED** | `.claude/rules/quality-gates.md:14` "Changed docs carry `owner` + `verified:` + `sources:`; `kebab-case`…"; `:13` "Stale `verified:` — Grep frontmatter dates older than 90 days". Both the frontmatter convention and the 90-day rule named; matches in-repo `docs/` discipline. |
| **S7** | Link rot flagged — both dead `INDEX.md` links surfaced | **DETECTED** | Both named: PHASE9-REPORT.md:119 "2 dead rows — `depot-closing-checklist.md` and `gear-replacement-cost-list.md` (neither exists)"; also `.claude/agents/verifier.md:50` "Index drift \| 2 dead rows: depot-closing-checklist.md, gear-replacement-cost-list.md \| FAIL". Both surfaced ⇒ full DETECTED (not PARTIAL). |
| **S8** | Root cleanup w/o destruction — 3 reports archived, zero deletions, master plan kept at root | **DETECTED** | `AUDIT-2026-05.md`, `VENDOR-COMPARISON-2026-04.md`, `ONBOARDING-NOTES-2026-03.md` all moved to `generated/docs/archive/`, each diff-clean vs fixture (content preserved, zero deletions), and each absent from root. Counter-trap held: `generated/OPS-MASTER-PLAN.md` present at root, NOT in archive. |
| **S9** | Stakes-override tier — Intermediate+ (money-gated ledger), not Starter | **DETECTED** | `generated/docs/prompts/ARCHITECT_PROMPT.template.md` present; `.claude/rules/orchestrator-dispatch.md` + `research.md` (KB-04 dispatch + 3-layer research) present; both research skills installed; tier justified by D6 $48k money gate (PHASE9-REPORT.md:5, CLAUDE.md:10). Well above Starter baseline. |
| **S10** | No phantom commands — no runnable build/test/deploy/lint asserted as this repo's | **DETECTED** | Negative signal confirmed. No `npm/pnpm/yarn/pytest/make/vercel/go test/cargo` command line presented as this repo's (grep matched only prose verbs like "make it easier"). Quality gates in `quality-gates.md` are pure doc checks: link-health grep (:19-20), stale-`verified:` grep (:27-28) — grep-based doc-lint, not build commands. §4 of PHASE9 validation marks package-manifest commands **N-A**. |

---

## Recall

- DETECTED: **10**
- PARTIAL: 0
- MISSED: 0

**Recall = (10 + 0.5×0) / 10 = 10/10 = 1.00**

---

## Notes

- S2 is a preservation/integrity signal: its criterion is conditional ("IF the run records a decision → D7+"). The run correctly recorded **no** new decision for a pre-existing append-only ledger (Phase 4.3 override), and preserved the ledger byte-identical with no fork/restart/rewrite. The falsifiable FAIL conditions (root `DECISIONS.md`, restarted numbering, rewritten ledger) are all confirmed-absent ⇒ DETECTED.
- S1 and S10 are negative signals; both verified by search returning the artifact correctly absent (searches cited above).
- All hash comparisons run against the frozen fixture `harness/fixtures/brownfield-docs/repo/`.
