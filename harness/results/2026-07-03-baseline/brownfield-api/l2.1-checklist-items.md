# L2.1 — VALIDATION_CHECKLIST Scoring — brownfield-api (SlotHarbor)

**Fixture**: brownfield-api · **Project**: SlotHarbor (FastAPI + SQLAlchemy 2.0 sync + Pydantic v2 + Alembic)
**Tree scored**: `<KIT_ROOT>/harness/results/2026-07-03-baseline/brownfield-api/generated/`
**Applicability inputs**: `maturity_tier=Intermediate`, `ide_scope=["claude-code"]`, `presets=["fastapi"]`, `packs=["kb-04","kb-05-intermediate"]` (manifest + PHASE9-REPORT).
**Scorer**: Opus 4.8 L2.1 subagent (no answer key). Static execution; live-install/network items → N/A with reason.

---

## Per-section status table

| § | Section | Status | One-line basis |
|---|---------|--------|----------------|
| 1 | Token Budget Compliance | PASS | CLAUDE.md 98 lines ≤150; all 6 core sections present; rules within budget (max convention 34, max router 73). |
| 2 | No Redundancy | PASS (caveat) | No rule/skill misplacement; dev-commands overlap AGENTS.md↔CLAUDE.md is template-mandated for a single-IDE project (see detail). |
| 3 | All Referenced Paths Exist | PASS | All rule dir-paths, cited files, and globs (`app/**`, `migrations/**`) resolve on disk. |
| 4 | All Commands Are Valid | PASS | Python project (no package.json); every command is a real binary (pip/uvicorn/alembic); pip prefix consistent; no JS-PM leakage. |
| 5 | No Secrets or Credentials | PASS | Value-shaped secret scan empty; `${DATABASE_URL}` placeholder used; `.env.example` carries names only (DATABASE_URL line is a dummy example connstring, not a real credential). |
| 6 | Conventions Match Codebase | PASS | 0 `async def`/`AsyncSession`, 0 Pydantic-v1 idioms, 0 `session.query()` in `app/` — rules match reality. |
| 7 | MCP Security Annotations Present | PASS | 3 servers each annotated (posture+creds); `.mcp.json`+settings.json strict-parse; pinned versions; `${ENV_VAR}`. snyk-scan item → N/A (network/token). |
| 8 | Skills Include Gotchas Sections | PASS (caveat) | All 8 decision skills + research-prompt-writer have `## Gotchas` (≥3 items) + `## When NOT`. research-synthesizer uses `## Anti-patterns`(6)+`## When NOT` verbatim from kit template (documented deviation). |
| 9 | Agents Include Explicit Constraints | PASS | All 5 agents: `## Constraints` + `tools:` frontmatter + one-sentence role + defined output format + least-privilege tools. |
| 10 | File Count and Completeness | PASS | AGENTS/CLAUDE/DECISIONS/.mcp.json present; 8 commands (≥3); Cursor items N/A (Claude-only); `.mcp.json` present. |
| 11 | Execution Infrastructure | PASS | 8 commands (≥5); settings.json 19-allow/5-deny + `hooks:{}` (no formatter, noted); mandatory trio present; feedback chain + Plan mode in CLAUDE.md. |
| 12 | Decision Skills Installation (KB-05) | PASS | 5 BLOCKER + 3 HIGH present; MEDIUM+council correctly absent (Advanced-only); 5/5 frontmatter fields each; router rule present. |
| 13 | KB-04 Decision Engineering | PASS | ARCHITECT template + orchestrator-dispatch + research rules + 2 research skills present; DECISIONS Tier 2 (3 entries, each w/ reversal triggers + re-verify). |
| 14 | Surface Routing Integration | N/A (pending operator) | `.claude/rules/ai-workflow.md` absent; Phase 4.5 deferred to a Cowork session per report — honest deferral, not a failure. |
| 15 | Vigilance Discipline | N/A (tier) | Advanced-only, opt-in; project is Intermediate; Phase 7 not offered. |
| 16 | Mission-Dispatch Pattern | N/A (tier) | Advanced-only, opt-in; project is Intermediate; Phase 8 not offered. |
| 17 | Cross-Artifact Consistency | PASS | Stack versions agree across all docs; generated↔generated refs resolve; manifest integrity (38 files, all paths exist, 3/3 sha256 match, metadata matches Phase 9); DECISIONS↔rules coherent. |

**Applicable sections (1–13, 17) = 14. N/A = 3 (§14 pending-operator, §15 tier, §16 tier).**
**Result: 14 / 14 applicable sections PASS.**

---

## Per-item detail with command outputs

### §1 Token Budget Compliance — PASS
- CLAUDE.md ≤150: `wc -l CLAUDE.md` → **98** ✓
- Six core sections: loop over "Build & Test Commands / Architecture / Key Conventions / Common Pitfalls / Session Protocol / Definition of Done" → **no MISSING output** (all six present as `##` headers) ✓
- base.md content lines: `.cursor/rules/base.mdc` N/A (Claude-only). Claude equivalent `.claude/rules/base.md` = 34 lines (within convention ≤40) ✓
- Total alwaysApply budget: Claude rules are not YAML-`alwaysApply`; convention rules 30–34 lines each, routers 42–73. Rule budget (convention ≤40, router ≤100) satisfied: `wc -l .claude/rules/*.md` max convention 34, max router 73 ✓

### §2 No Redundancy — PASS (caveat)
- No rule content duplicated between base.md and domain rules (base = execution protocol/hard constraints; api-python = FastAPI/Pydantic/SQLAlchemy conventions) ✓
- No skill content that belongs in a rule ✓
- **Caveat — dev commands overlap**: the four dev commands (`pip install .`, `uvicorn app.main:app`, `alembic upgrade head`, `alembic revision`) appear in BOTH `AGENTS.md` §Dev Commands AND `CLAUDE.md` §Build & Test Commands. Checklist §2 line "No command appears in both CLAUDE.md and AGENTS.md unless they differ between IDEs" is in tension here. Adjudicated PASS because: (a) the kit's own `templates/shared/AGENTS.md.template` mandates a `## Dev Commands` section and CLAUDE.md's `Build & Test Commands` is a REQUIRED §1 core section — both are structural template features, not generation drift; (b) the "differ between IDEs" nuance targets multi-IDE projects; this is Claude-only, so AGENTS.md + CLAUDE.md are both consumed by the same agent and the canonical-commands duplication is by design. Flag retained as a checklist-internal tension, not a fixture defect.

### §3 All Referenced Paths Exist — PASS
- Rule dir-path extraction loop → **no MISSING DIR** ✓
- Cited files (`app/main.py`, `app/db.py`, `app/config.py`, `app/models.py`, all 3 routers, `migrations/env.py`, `alembic.ini`, `pyproject.toml`) → **no MISSING FILE** ✓
- Globs `app/**/*.py` + `migrations/**/*.py` (api-python.md frontmatter scope) → `app/`, `migrations/`, `migrations/versions/` all exist ✓
- Dead-rule lint repeatability: §3/§4 checks are re-runnable (grep-based); WORKFLOW_HEALTH_AUDIT lens-3 named in kit docs ✓

### §4 All Commands Are Valid — PASS
- No `package.json` (Python project) → npm-scripts item N/A; commands are direct binaries ✓
- Every command in CLAUDE.md / AGENTS.md / base.md is `pip`/`uvicorn`/`alembic` — all real ✓
- Migration commands match ORM CLI: Alembic → `alembic upgrade head` / `alembic revision --autogenerate` ✓
- PM prefix consistent: `pip` throughout; `grep npm/yarn/pnpm` in docs → **none** ✓

### §5 No Secrets or Credentials — PASS
- Value-shaped secret scan (`sk-…|AKIA…|ghp_…|Bearer …|password=…|secret=…`) over CLAUDE/AGENTS/.claude/.mcp.json/.mcp.annotations/manifest/.env.example → **no matches** ✓
- `.mcp.json` uses `"DATABASE_URL": "${DATABASE_URL}"` (placeholder) ✓
- `.env` never read (report §Gaps; only `.env.example` merged) ✓
- Minor observation (not a failure): `.env.example` line 6 ships a dummy connection string `postgresql+psycopg://user:password@localhost:5432/slotharbor`. It is a placeholder illustrating URL shape (`user`/`password` literals), not a real credential; does not match the checklist's `password=` value-shaped pattern. Standard `.env.example` practice. SECRET_KEY/WAREHOUSE_CODE/MAX_PAGE_SIZE have empty RHS.

### §6 Conventions Match Codebase — PASS
- `grep async def|AsyncSession|create_async_engine app/` → **0** (matches sync rules + DECISIONS #1) ✓
- `grep 'class Config|.dict()|@validator' app/` → **0** (matches Pydantic-v2 rules) ✓
- `grep session.query app/` → **0** (matches SQLAlchemy-2.0 rules) ✓
- File naming: `snake_case.py` per rules ✓; import structure = `app.` package (matches `[tool.setuptools] packages=["app"]`) ✓
- Test framework: none installed → rules correctly assert no test tooling (matches pyproject) ✓

### §7 MCP Security Annotations Present — PASS
- 3 servers (`github` RW-OAuth / `context7` RO / `postgres` RO) each have a `.mcp.annotations.md` entry: posture + credentials + add/skip-when ✓
- `python3 -m json.tool < .mcp.json` → **OK**; `.cursor/mcp.json` N/A (Claude-only); `.claude/settings.json` strict-parses → **OK** ✓
- All creds `${ENV_VAR}` (`${DATABASE_URL}`) ✓
- No `@latest`; pinned `@upstash/context7-mcp@3.2.2`, `mcp-postgresql-ops@3.2.8`; github remote-HTTP upstream-managed ✓
- Correct filenames: `.mcp.json` (Claude Code) present; no `.cursor/mcp.json` needed ✓
- **N/A item — `uvx snyk-agent-scan@latest`**: requires network + optional `SNYK_TOKEN`; not runnable in static scoring env. Report records it RAN degraded-to-inventory during generation; the 3 servers are registry-vetted with documented postures. N/A (network/token), not a section blocker.

### §8 Skills Include Gotchas Sections — PASS (caveat)
- Decision-skill `## Gotchas` item counts: pre-mortem 5, steelman 5, confidence-calibration 4, reversibility-check 4, anti-sycophancy-meta 6, inversion 5, second-order-thinking 5, disconfirming-evidence-first 5 — all ≥2 ✓
- `## When NOT` present on all 8 decision skills ✓
- research-prompt-writer: `## Gotchas` = 3 items + `## When NOT` ✓
- Trigger written for model: all 8 decision skills carry a model-directed `description:` frontmatter ✓
- Steps numbered/sequential in skill procedures ✓
- **Caveat — research-synthesizer**: has NO literal `## Gotchas` header; uses `## Anti-patterns` (6-row table) + `## When NOT to invoke`. This is a literal miss on the "`## Gotchas` section" wording but is **verbatim from the kit's shipped `research-synthesizer` template** (a Layer-2/3 procedural skill with its own section structure) — a template convention, not a generation error. Section scored PASS with the deviation documented (matches PHASE9-REPORT "benign variance").

### §9 Agents Include Explicit Constraints — PASS
All 5 agents (simplifier, reviewer, verifier, tester, security-auditor):
- `## Constraints` section with NEVER/ALWAYS rules → present (1 each) ✓
- `tools:` frontmatter (explicit tool access) → present (1 each) ✓
- Role = one sentence (`**Role**: …`) ✓
- Output format defined (fenced report template each) ✓
- Least-privilege: simplifier/reviewer Read+Grep (no write); verifier Bash(scoped app-boot+alembic)+Read; tester Write scoped to tests/ + Bash pytest-only; security-auditor read-only Bash. No agent has write access it doesn't need ✓

### §10 File Count and Completeness — PASS
- AGENTS.md ✓ · CLAUDE.md ✓ · DECISIONS.md at root ✓ · `.mcp.json` ✓
- Cursor items (base.mdc / domain .mdc / .cursor/mcp.json / .cursorindexingignore / .cursorignore) → **N/A** (`ide_scope=["claude-code"]`; no `.cursor/`) ✓
- Skills created only for applicable procedures (decision + research) ✓
- ≥3 commands: **8** present (review-pr, update-context + discover-mcp/security-review/tech-debt/workflow-help/status/deploy; deploy justified — Docker+Heroku deployable) ✓

### §11 Execution Infrastructure — PASS
- Slash commands ≥5: **8** ✓
- PostToolUse hooks: `.claude/settings.json` `hooks: {}` with report note "no formatter installed" — allowed form ✓
- Permissions: `allow`=19 (git reads, build, lint-absent, test-absent, pip/alembic/docker) + `deny`=5 (`rm -rf`, `git push --force`, `git reset --hard`, `.env`, `.env.*` reads) ✓
- MCP servers configured: `.mcp.json` has 3 project-relevant servers (github, context7, postgres) ✓
- Feedback-loop chain: CLAUDE.md Definition of Done (boot + migration-apply + manual endpoint exercise) — concrete, adapted to no-tooling reality ✓
- Three mandatory subagents: Simplifier + Reviewer + Verifier all in `.claude/agents/` ✓
- Plan mode in session protocol: CLAUDE.md line 58 "use Plan mode to align on approach" ✓

### §12 Decision Skills Installation (KB-05) — PASS
- 5 BLOCKER (pre-mortem, steelman, confidence-calibration, reversibility-check, anti-sycophancy-meta) each at `.claude/skills/decision/<name>/SKILL.md` → **no MISSING** ✓
- 4 HIGH: inversion, second-order-thinking, disconfirming-evidence-first present (steelman already in BLOCKERs) → 3 net-new present ✓
- 3 MEDIUM (10-10-10, cost-of-inaction, bayesian-update) → **absent** (correct; Advanced-only) ✓
- Council (council-3-voice) → **absent** (correct; Advanced AND Max-only) ✓
- Frontmatter: each SKILL.md has 5/5 `name`+`description`+`severity`+`confidence`+`surface` ✓
- Router rule `.claude/rules/decision-skills.md` present: documents 8 skills + phrase-mapping router + anti-sycophancy auto-fire + LLM fallback + Type-1 non-council fallback (steelman+pre-mortem) ✓

### §13 KB-04 Decision Engineering (Intermediate+) — PASS
- `docs/prompts/ARCHITECT_PROMPT.template.md` present ✓
- `.claude/rules/orchestrator-dispatch.md` present (Cursor mirror N/A) ✓
- `.claude/rules/research.md` present (Cursor mirror N/A) ✓
- `.claude/skills/research-prompt-writer/SKILL.md` present (Cursor mirror N/A) ✓
- `.claude/skills/research-synthesizer/SKILL.md` present (Cursor mirror N/A) ✓
- DECISIONS Tier 2: 3 entries, each with "Reversal triggers" (3) + "Re-verify" (3) → Tier 2 format satisfied ✓

### §14 Surface Routing Integration — N/A (pending operator)
- `.claude/rules/ai-workflow.md` **absent**. Report §Validation §14 marks this "PENDING — operator action": Phase 4.5 was intentionally deferred to a fresh Cowork session (integration prompt handoff block provided). Checklist §14 explicitly permits: "If Phase 4.5 was deferred… flagged 'pending operator action' rather than failed." → N/A (honest deferral).

### §15 Vigilance Discipline — N/A (tier)
- Advanced-tier-only, opt-in. Project is Intermediate; Phase 7 not offered (report: "N/A — Intermediate tier"). Checklist gates this section to Advanced. → N/A (tier).

### §16 Mission-Dispatch Pattern — N/A (tier)
- Advanced-tier-only, opt-in. Project is Intermediate; Phase 8 not offered. → N/A (tier).

### §17 Cross-Artifact Consistency — PASS
- Fact agreement: FastAPI 0.115.6 / SQLAlchemy 2.0.31 / Pydantic 2.7.4 / Python >=3.12 identical across CLAUDE.md, AGENTS.md, rules, pyproject.toml (shortened forms "0.115"/"2.0" are consistent abbreviations, no version conflict) ✓
- Generated↔generated refs: all `.claude/{rules,skills,agents}/…` cross-references resolve → **no BROKEN REF** ✓
- Manifest integrity: `python3 -m json.tool .ai-kit-manifest.json` OK; 38 files, all listed paths exist, 3/3 spot-checked sha256 MATCH; `maturity_tier=Intermediate`, `ide_scope=["claude-code"]`, `presets=["fastapi"]` all match Phase 9 report ✓
- Doc-derived checks executed: PHASE9-REPORT records per-doc falsifiable checks (CLAUDE 4/4, AGENTS 4/4, decision-skills router resolves) ✓
- DECISIONS↔rules coherence: legacy_export.py framed as "retained/unmounted pending removal" consistently in DECISIONS #2, base.md, CLAUDE.md — no rule mandates a pattern a reserved entry contradicts ✓

---

## Sign-off

```
Project:          SlotHarbor (fixture: brownfield-api)
Generator run:    2026-07-03
Checklist passed: yes — 14/14 applicable sections PASS
Sections N/A:     §14 (pending operator), §15 (tier), §16 (tier)
Items failed:     none
Documented deviations (non-failing): §2 template-mandated command overlap;
                  §8 research-synthesizer uses ## Anti-patterns not ## Gotchas (verbatim kit template)
N/A items within passing sections: §7 snyk-agent-scan (network/token)
Validator:        Opus 4.8 L2.1 checklist scorer (no answer key)
```
