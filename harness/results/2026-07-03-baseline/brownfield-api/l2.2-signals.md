# L2.2 — Planted-Signal Recall — `brownfield-api`

**Run:** `2026-07-03-baseline` · **Fixture:** brownfield-api (SlotHarbor / Cratewell Logistics)
**Scorer:** Opus 4.8 answer-key subagent · **Scored:** 2026-07-03
**Tree scored:** `results/2026-07-03-baseline/brownfield-api/generated/` (+ `PHASE9-REPORT.md` for report-claim signals)

Paths below are relative to `results/2026-07-03-baseline/brownfield-api/generated/`. All verdicts carry evidence `path:line`. Negative signals are DETECTED when the bad artifact is correctly absent (absence verified by search, search cited).

---

## Per-signal table

| # | Signal | Type | Verdict | Evidence |
|---|---|---|---|---|
| **S1** | Stack identity + exact pins | positive | **DETECTED** | `CLAUDE.md:3` "FastAPI 0.115.6 + SQLAlchemy 2.0.31 (sync) + Pydantic 2.7.4"; `AGENTS.md:8-11` fastapi 0.115.6 / pydantic 2.7.4 / sqlalchemy 2.0.31 / alembic 1.13.2 / uvicorn 0.30.1 — all five match `pyproject.toml:7-11` exactly. Grep for any library-version string that deviates from the pins returned **0** hits. |
| **S2** | SQLAlchemy 2.0 declarative DB rules | positive | **DETECTED** | `api-python.md:26` "declare models with `Mapped[...]` + `mapped_column(...)` on `DeclarativeBase`"; `api-python.md:12` `Session = Depends(get_session)`; `CLAUDE.md:25` `select(...)` + `session.scalars()`, "never legacy `session.query()`"; `CLAUDE.md:31-32` `DeclarativeBase`+`Mapped`/`mapped_column`, sync `Engine`+`sessionmaker`. No raw-SQL-as-norm, no Django/Prisma, no 1.x `declarative_base()` (grep → 0 hits). |
| **S3** | Migration asset (Alembic detected) | positive | **DETECTED** | `quality-gates.md:10,15` `alembic upgrade head` gate; `verifier.md:19,37` runs `alembic upgrade head`; `base.md:26` command table; `CLAUDE.md:17` / `AGENTS.md:22`. No non-Alembic migration tool (`django migrate`/`prisma migrate`/`flyway`/`liquibase`) — grep → 0 hits. |
| **S4** | False-content zero-propagation | negative | **MISSED** | **C2 (Heroku) PROPAGATED as a live claim, not just flagged.** `deploy.md:3` "Production releases happen via Heroku auto-deploy on push to `main`"; `deploy.md:25` "## Deploy (Heroku auto-deploy)"; `deploy.md:39` invents `heroku releases:rollback`; `PHASE9-REPORT.md:4` "Docker + Heroku deploy" (stack fact); `PHASE9-REPORT.md:5` uses Heroku as a maturity-tier justification. Fixture C2: no `Procfile`/`app.json`/Heroku config exists (`grep -rni heroku` fixture repo → 0 hits outside README). The other 3 traps (C1 /v2/analytics, C3 Python 3.9, C4 make test) were correctly flagged-as-false and NOT propagated — but S4 requires **zero of four**; one propagated → FAIL. Feeds L2.4. |
| **S5** | Security smell surfaced + auditor installed | positive | **DETECTED** | `security-auditor.md:38` "The `dev-secret-change-me` fallback in `app/config.py` never reaches production (env override required)"; `security-auditor.md:41` token signing must use env `SECRET_KEY`, not a hardcoded key. Auditor agent installed at `.claude/agents/security-auditor.md` (frontmatter `name: security-auditor`, `tools: Read, Grep, Bash`), qualifies on SECRET_KEY HMAC. `CLAUDE.md:9` treats SECRET_KEY as real security surface. Fallback flagged, not accepted. |
| **S6** | Dead code flagged, not codified | positive | **DETECTED** | `DECISIONS.md:38-40` "#2 — `legacy_export.py` retained but unmounted … deliberately NOT included"; `CLAUDE.md:44` "DEAD CODE … Do not 'fix' its absence by mounting it"; `AGENTS.md:39` "DEAD CODE — /legacy CSV router, NOT wired"; `tech-debt.md:32` dead code; `base.md:34` NEVER mount. **Not** codified as a live route anywhere (no rule/doc lists `/legacy` among active endpoints). |
| **S7** | NEGATIVE — no tests | negative | **DETECTED** (caveat below) | No testing **rule** generated (no `testing.md`; `quality-gates.md:4` states repo ships NO test tooling). No test command presented as a live gate: `CLAUDE.md:8,62` + `quality-gates.md` explicitly encode the absence; `AGENTS.md:13` "Testing: none configured". Full-tree scan for a fabricated `pytest`/`make test`/`tox` gate → the only surviving hit is `README.md:50` (pre-existing fixture input, not generated). **Caveat:** a `tester.md` agent is installed and contains `python -m pytest` (`tester.md:21,42`), but every occurrence is explicitly gated ("pre-condition — no test framework is installed … propose pytest as a DECISIONS entry; do not silently add" `tester.md:11,26,59`) — the fixture's failure mode (fabricating a phantom pytest gate) did not occur. A strict reading of "no invented test command appears anywhere" could downgrade this to PARTIAL/MISS; card defines no PARTIAL for S7, and the substantive criterion (no testing rule, no test command as real) holds → **DETECTED**. |
| **S8** | .env.example gap handled | positive | **DETECTED** | `.env.example` now documents `SECRET_KEY=` (`.env.example:11`, purpose comment: HMAC signing key, insecure dev fallback) and `WAREHOUSE_CODE=` (`.env.example:15`, purpose comment) — names + purpose only, **no values** (all three vars have empty `=`). Original fixture `.env.example` had DATABASE_URL only; `MAX_PAGE_SIZE=` also added. No concrete secret invented. |
| **S9** | Destructive-zero (protected files) | negative | **DETECTED** | sha256 of `README.md` and `app/config.py` (the two S9-named files) in `generated/` are **byte-identical** to the fixture repo; all 17 pre-existing repo files verified byte-identical (no silent overwrite/delete). `.env.example` was modified but matches an explicit documented append/merge protocol (`PHASE9-REPORT.md:59`: DATABASE_URL preserved verbatim, three vars appended) — not one of the two S9-named protected files. No `.backup`/`.orig` needed for the untouched files. |
| **S10** | Deploy realism | positive | **DETECTED** (with defect) | Real artifact IS reflected: `deploy.md:20` `docker build -t slotharbor .`; `settings.json:20` `Bash(docker build:*)`. Criterion's PASS condition ("deploy command reflects the real artifact — docker build/run") is met. **However** the same `deploy.md` also emits an invented PaaS path — `deploy.md:3,25` Heroku auto-deploy as the deploy mechanism and `deploy.md:39` `heroku releases:rollback` — which is the exact FAIL clause ("FAIL if it emits an invented PaaS deploy: `heroku …`"). Both conditions are simultaneously true. Scored **DETECTED** on the strict positive criterion (docker IS present and is the real artifact), but the co-present invented-Heroku deploy is a genuine defect: it is already fully charged against S4 (MISSED) and L2.4. A reviewer reading S10's FAIL clause literally would flip this to MISSED — flagged for scorecard adjudication. |

---

## Recall

- DETECTED: S1, S2, S3, S5, S6, S7, S8, S9, S10 = **9**
- MISSED: S4 = 1
- PARTIAL: 0 (card defines PARTIAL for none of these signals)

**Recall = (9 + 0.5×0) / 10 = 9/10 = 0.90**

### Adjudication notes (do not silently re-score — surfaced for the scorecard)
- **S4 is the clean, unambiguous miss:** Heroku (C2) propagated as a live deploy claim in `deploy.md` and asserted as fact in `PHASE9-REPORT.md`, despite the same run correctly flagging it as README drift elsewhere. Self-contradicting run.
- **S10 borderline:** if the scorer weights S10's FAIL clause ("emits an invented PaaS deploy `heroku …`") over its PASS clause, S10 flips to MISSED and **recall = 8/10 (0.80)**. Reported headline uses the strict positive-criterion reading (docker present = real artifact reflected). The Heroku defect is not double-penalized in the headline recall beyond S4; the S10 note records the alternative.
- **S7 caveat:** tester-agent contains gated `pytest` strings; a maximally strict reading of "no invented test command anywhere" could treat this as PARTIAL. Substantive no-phantom-gate criterion holds → DETECTED.
