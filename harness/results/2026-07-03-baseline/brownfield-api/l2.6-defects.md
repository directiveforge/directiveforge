# L2.6 — Workflow-Defect Review · fixture `brownfield-api` (SlotHarbor)

**Reviewer:** Opus 4.8, independent adversarial (L2.6, step-④ falsification, fresh context)
**Run under review:** `harness/results/2026-07-03-baseline/brownfield-api/generated/`
**Baseline compared against:** pristine source `harness/fixtures/brownfield-api/repo/`
**Answer key:** NOT consulted (FIXTURE-CARD and fixture internals off-limits per brief)
**Spec:** HARNESS-SPEC.md §5 L2.6 — 8 defect classes (a)–(h), one primary class per defect, severity 🚨/⚠/🟡, verbatim evidence + `file:line` mandatory.

---

## Summary counts

| | Count |
|---|---|
| **Total findings** | 2 |
| 🚨 Critical | 0 |
| ⚠ Warning | 0 |
| 🟡 Minor | 2 |

**Per class:** (a) phantom-reference 0 · (b) contradiction **1** · (c) dead-asset 0 · (d) stale-content **1** · (e) scope-misroute 0 · (f) governance-violation 0 · (g) placeholder-leak 0 · (h) duplication-drift 0.

This is a clean generation. Both findings are 🟡 and both live in reporting/narrative prose, not in an operative workflow asset — no generated rule, agent, command, skill, or config fires wrong.

---

## Findings by class

### (b) contradiction — 1 finding

| # | Sev | File:line | Defect | Verbatim evidence | Verification |
|---|-----|-----------|--------|-------------------|--------------|
| B1 | 🟡 | `PHASE9-REPORT.md:84` | Self-count mismatch: prose says "the 3 analysis-derived files" then enumerates **four** items (CLAUDE.md, api-python.md, decision-skills.md, **and** the merged .env.example). | *"Template + template_sha256 recorded per file (null for the **3** analysis-derived files: CLAUDE.md, api-python.md, decision-skills.md, **and the merged .env.example**)."* | Verified against `.ai-kit-manifest.json`: exactly **4** entries carry `"template": null` — `CLAUDE.md` (created), `.env.example` (merged-existing), `.claude/rules/api-python.md` (created), `.claude/rules/decision-skills.md` (created). The count word "3" contradicts its own four-item list and the manifest. Doc↔doc + doc↔manifest contradiction. Cosmetic (a generation-report artifact, not a workflow asset the agent consults at runtime). |

### (d) stale-content — 1 finding

| # | Sev | File:line | Defect | Verbatim evidence | Verification |
|---|-----|-----------|--------|-------------------|--------------|
| D1 | 🟡 | `CLAUDE.md:9` (also `AGENTS.md:12`, `.claude/agents/security-auditor.md:40-42`, `.claude/commands/deploy.md:23`) | The workflow elevates `SECRET_KEY` "appointment-token HMAC" into a **live** security surface (auditor checklist items on constant-time HMAC compare, token-signing provenance), but **no token-signing / HMAC code exists anywhere in the repo** — `SECRET_KEY` is declared in `app/config.py` and never read again. | CLAUDE.md:9 — *"`SECRET_KEY` signs appointment-token HMACs. It is real security surface."*  security-auditor.md:42 — *"Signed tokens are verified (constant-time compare) before trust — flag any `==` comparison of HMACs"* | `grep -rniE 'hmac\|\.sign\|token\|jwt' app/` returns **only** the `app/config.py:17` comment; `grep 'SECRET_KEY' app/` returns only the `os.getenv` declaration at `config.py:20`. So the "real security surface" claim describes behavior that does not yet exist in code. **Downgraded to 🟡, NOT filed as false-content:** the claim traces verbatim to the source's own comment (`app/config.py:17`: *"Signing key for the internal appointment-token HMAC"*), so the generator faithfully propagated a planted source assertion rather than inventing one, and the auditor items are correctly phrased conditionally ("flag any…", "verify any…"). The residual risk is that a reader treats a never-used key as an active audited control. Grounded-but-aspirational, hence stale-content-risk not fabrication. |

### Classes with ZERO findings (named explicitly per v0.16.0 precedent)

- **(a) phantom-reference — 0.** Every kit-absolute path referenced across CLAUDE.md, AGENTS.md, all six rules, all commands, and both research skills resolves on disk (14/14 checked: KB-01/02/04/05, MCP-SERVER-REGISTRY, VALIDATION_CHECKLIST, presets/fastapi.md, CLAUDE-SURFACE-ROUTING, WORKFLOW-CLAUDE-CODE, code-handoff-prompts, cowork-browser-operations, research/2026-05-26-decision-techniques, HANDOFF template, CLAUDE_SURFACE_ROUTING_INTEGRATION_PROMPT). Every intra-project ref (`.claude/rules/*`, `.claude/skills/decision/<name>`, `app/db.py`, `app/config.py`, `app/models.py`, DECISIONS #1/#2/#3) resolves. Every command is real (`pip install .`, `uvicorn app.main:app`, `alembic upgrade head`, `docker build`, `heroku releases:rollback`). Model-constraint names cited in `tester.md` (`uq_dock_wh_label`, `uq_booking_dock_start`) match `app/models.py:45,63` exactly.
- **(c) dead-asset — 0.** `api-python.md` frontmatter `globs: ["app/**/*.py", "migrations/**/*.py"]` — both dirs exist with `.py` files, glob fires. No unpassable gate: `quality-gates.md` gates (app-boot, migration-apply, migration-review) are all runnable and honestly scoped to what exists. No placeholder-riddled asset. All 8 decision skills + 2 research skills are fully materialized (94–147 lines each).
- **(e) scope-misroute — 0.** Preset `fastapi` correct; tier `Intermediate` justified (Docker + Heroku prod deploy, "defensible decisions" operator signal); `ide_scope: ["claude-code"]` honored — no `.cursor/` tree generated. The only "cursor" strings are (i) manifest `template:` provenance fields pointing at `templates/cursor/skills/…` (the kit's canonical source location for shared skill templates — installed to `.claude/skills/`, correct) and (ii) PHASE9 narrative explaining the deliberate `.cursor/` omission. Neither is an IDE leak. Council correctly withheld (Advanced+Max only); vigilance/mission-dispatch correctly N/A at Intermediate.
- **(f) governance-violation — 0.** README.md is **byte-identical** to source (README drift surfaced as CLAUDE.md pitfalls + `/tech-debt` findings + DECISIONS, never silently edited — the "codebase wins, propose-don't-mutate" contract). All app/ source (`main.py`, `config.py`, `models.py`, routers, migrations) byte-identical to pristine. `.env.example` is the sole modified pre-existing file and is a **legitimate additive merge** (preserved the `DATABASE_URL` line verbatim; appended `SECRET_KEY`/`WAREHOUSE_CODE`/`MAX_PAGE_SIZE` names the code reads, no values) — disposition correctly recorded `merged-existing` in the manifest. No pre-existing file deleted, overwritten, or renumbered.
- **(g) placeholder-leak — 0.** `grep '{{'` across all non-template output returns nothing. The only `{{…}}` residue is in `HANDOFF.md` (`{{CURRENT_STATE}}` etc.) and `docs/prompts/ARCHITECT_PROMPT.template.md` — both are **intended template files**: HANDOFF's slots are owned by `/status` (status.md:26 rewrites only the machine section) and the report declares "Template only — session-fill slots retained"; ARCHITECT is explicitly a `.template.md`. No stray lorem/stub/TODO copy (the three `grep` hits for "placeholder/TODO" are all legitimate self-referential prose: the manifest `"placeholders"` key, a PHASE9 "no placeholders" claim, and a quality-gates policy line forbidding them).
- **(h) duplication-drift — 0.** Fact-ownership is cleanly partitioned: the canonical stack/commands table lives in AGENTS.md; CLAUDE.md carries a `<!-- Canonical stack table lives in AGENTS.md; these are quirks only. -->` pointer (CLAUDE.md:13) and repeats only quirks. Sync-only, legacy-export-unmounted, and os.getenv-config facts recur across CLAUDE.md / AGENTS.md / base.md / api-python.md / DECISIONS but are **identical in every instance** (verified: all say sync SQLAlchemy 2.0, all cite DECISIONS #1/#2/#3, all say Python ≥3.12 with README "3.9+" stale) — consistent repetition for reinforcement, zero divergence.

---

## Methodology note

I read every generated file (39 artifacts) and every pristine source file, then ran mechanical cross-checks: (1) existence of all 14 kit-absolute paths + all intra-project references; (2) every documented command against the actual `pyproject.toml`/Dockerfile/README; (3) glob-frontmatter validity against the real directory tree; (4) `grep` for `{{…}}` / lorem / stub / TODO residue outside intended template files; (5) byte-level diff of every pre-existing path (source vs generated) to catch governance violations; (6) fact-duplication audit across CLAUDE.md/AGENTS.md/rules for divergence; (7) manifest integrity (38 listed paths all exist, self-excluded, `template: null` set cross-checked); (8) code-grounding of every load-bearing security/architecture claim via `grep` in `app/`. Each candidate was re-opened and re-verified before filing; two candidates that first looked like defects — the `.claude/rules/` glob frontmatter (confirmed a supported kit mechanism per VALIDATION_CHECKLIST §3) and the HANDOFF `{{…}}` residue (confirmed governance-sanctioned template slots) — were dismissed on verification and are recorded above as zero-finding rationale. Threats to validity per HARNESS-SPEC §5: single-reviewer variance, and the a/d boundary — D1 sits on the phantom-reference↔stale-content edge and was assigned to stale-content (the referent *exists* as a declared key, it is merely never exercised) and softened to 🟡 because it faithfully mirrors a planted source comment rather than inventing a fact.
