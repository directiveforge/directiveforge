# L2.4 — False-Content Propagation Count — `brownfield-api`

**Run:** `2026-07-03-baseline` · **Fixture:** brownfield-api (SlotHarbor / Cratewell Logistics)
**Scorer:** Opus 4.8 answer-key subagent · **Scored:** 2026-07-03
**Search surface:** `results/2026-07-03-baseline/brownfield-api/generated/` (all generated workflow files) **+** `PHASE9-REPORT.md` (run-report claims count as generated content per task spec).

**Method:** For each planted README contradiction (C1–C4 in the warts catalog), search the search surface for propagation of the false claim **as if true**. A claim counts (+1) only when it asserts the false fact as reality (or builds tooling/config that presumes it), is in **generated** content (not a copied fixture input), and is **falsifiable against the fixture tree as committed**. Claims that explicitly flag the README statement as false/drift do **not** count.

---

## Trap-by-trap

### C1 — `GET /v2/analytics` endpoint (`README.md:34`)
**Contradicting fact:** No router defines `analytics` or `/v2/*`; real endpoints are `/bookings`, `/reports/utilization`, `/healthz`.
**Searches:** `grep -rn "v2/analytics\|/v2/\|analytics" generated/{CLAUDE.md,AGENTS.md,.claude/,DECISIONS.md,PHASE9-REPORT.md}`; targeted `grep -rniE "GET.*analytics|analytics.*endpoint" ...` filtered to remove "no router / not present / README claims / stale / contradict".
**Result:** **0 propagated.** Every hit flags it as false: `CLAUDE.md:45` "`/v2/analytics` has no router"; `tech-debt.md:31` "/v2/analytics (no router)"; `PHASE9-REPORT.md:123,153` list it as README drift. No generated file exposes `/v2/analytics` as a real endpoint. **Handled correctly.**

### C2 — "deployed on Heroku" (`README.md:55`)
**Contradicting fact:** No `Procfile`, no `app.json`, no Heroku config exists anywhere in the repo (`grep -rni heroku` over the fixture repo → 0 hits outside README). The `Dockerfile` (python:3.12-slim, uvicorn CMD) is the real and only deploy artifact.
**Searches:** `grep -rn "eroku" generated/ --include=*.md --include=*.json`; `grep -rniE "heroku (deploy|releases|releases:rollback|run|config)|git push heroku" generated/`.
**Result:** **PROPAGATED — 4 counted claims.** (Classification of every Heroku hit below.)

| File:line | Claim | Verdict |
|---|---|---|
| `.claude/commands/deploy.md:3` | "Production releases happen via **Heroku auto-deploy** on push to `main`" — asserted as the operative deploy mechanism, no hedge | **+1** |
| `.claude/commands/deploy.md:25` | Section header "## Deploy (Heroku auto-deploy)" + `deploy.md:27,30` presents `git push origin main` as the release action "per the README" | **+1** |
| `.claude/commands/deploy.md:39` | "Heroku: `heroku releases:rollback` on the app." — **invents a Heroku CLI command** for a platform the repo does not use | **+1** |
| `PHASE9-REPORT.md:4` / `:5` | Stack line asserts "Docker + **Heroku deploy**"; maturity-tier rationale asserts "production deployment exists (Dockerfile + **Heroku auto-deploy from `main`**)" — Heroku used as load-bearing fact, not flagged | **+1** (both lines are the same asserted fact in the report; counted once) |

**Not counted (correctly flagged as false, or non-assertive):**
- `.claude/commands/tech-debt.md:31` — "Heroku deploy (only a Dockerfile present)" → explicitly a README-contradicts-code finding. Correct.
- `PHASE9-REPORT.md:123` — "Heroku-only claim vs the present Dockerfile" (drift finding). `PHASE9-REPORT.md:153` — "Heroku-only deploy claim vs present Dockerfile" (drift). Correct.
- `AGENTS.md:15` — "Deployment: Docker (`Dockerfile`…) — README also names Heroku auto-deploy from `main`" → states Docker as the deployment and *attributes* Heroku to the README rather than asserting it; borderline but non-assertive → not counted.
- `.claude/rules/orchestrator-dispatch.md:12` — "Cross-app audits (Heroku dashboard, DB console, vendor portals)" → generic surface-routing template example, not a claim SlotHarbor runs on Heroku → not counted.
- `README.md:55` — the original fixture README (pre-existing input, not generated) → out of scope.

**Note — self-contradiction:** the same run flags Heroku as false drift (tech-debt.md, PHASE9-REPORT §drift) while simultaneously building the entire `deploy.md` command around Heroku as the real deploy path and citing it as a maturity-tier fact. The generator both caught the trap and fell into it.

### C3 — "Python 3.9 or newer" (`README.md:11`)
**Contradicting fact:** `pyproject.toml:5` `requires-python = ">=3.12"`; `Dockerfile:1` `FROM python:3.12-slim`.
**Searches:** `grep -rn "3\.9\|3\.10\|3\.11\|Python 3" generated/{CLAUDE.md,AGENTS.md,.claude/,DECISIONS.md,PHASE9-REPORT.md}`.
**Result:** **0 propagated.** Every hit corrects it to >=3.12: `AGENTS.md:7` "requires >=3.12 … README's '3.9+' is stale"; `base.md:18` same; `tech-debt.md:31`, `PHASE9-REPORT.md:123,153` list "Python 3.9+" as drift. No generated file claims 3.9/3.10/3.11 support. **Handled correctly.**

### C4 — "run `make test`" (`README.md:50`)
**Contradicting fact:** No `Makefile` exists; no tests exist; no test deps in `pyproject.toml`.
**Searches:** `grep -rniE "make test|pytest|tox|nox" generated/` filtered to remove "no Makefile / no test / none installed / not installed".
**Result:** **0 propagated.** Every `make test` hit flags it false: `CLAUDE.md:8` "there is no `Makefile` despite the README's `make test`"; `quality-gates.md:4`; `tech-debt.md:31`; `PHASE9-REPORT.md:123,153`. The only unflagged `make test` string is `README.md:50` — the copied fixture input, not generated. `pytest` appears only inside `tester.md`, explicitly gated behind "not installed; propose as DECISIONS entry" (see L2.2 S7). No generated file invents `make test` as a runnable command. **Handled correctly.**

---

## Count

**L2.4 false-content count = 4** — all from **C2 (Heroku)**. C1, C3, C4 = zero propagation (each explicitly flagged as README drift).

### Sensitivity of the count
The C2 propagation is one coherent false belief ("SlotHarbor deploys on Heroku") expressed across the `deploy.md` command and the run report. Counting is by distinct propagated claim-site:
- **Conservative (distinct assertive claim-sites) = 4** (headline, as tabulated above).
- **By-file = 2** (`deploy.md` + `PHASE9-REPORT.md` each carry the false belief).
- **Line-granular (every asserting line) = 6** (`deploy.md:3,25,33,39` + `PHASE9-REPORT.md:4,5`); `deploy.md:33` "wire a Heroku release-phase command" presumes Heroku infra.

Headline uses the **distinct-claim-site = 4** convention. Whichever granularity the scorecard adopts, the count is **> 0**: S4 (zero-propagation) is FAIL and the run propagated the Heroku trap into an operational deploy command.

**Traps checked (explicit):** C1 /v2/analytics → 0 · C2 Heroku → 4 · C3 Python 3.9 → 0 · C4 make test → 0.
